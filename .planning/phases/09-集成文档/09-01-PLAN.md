---
phase: 09-集成文档
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - INTEGRATION.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "INTEGRATION.md 文件已创建在项目根目录"
    - "文档包含完整的 CLI 调用方式说明(基本命令格式、全局参数、参数优先级)"
    - "文档包含完整的 JSON API 说明(--json-help, --quiet, --json-output)"
  artifacts:
    - path: "INTEGRATION.md"
      provides: "集成指南主文档"
      contains: "# 集成指南"
      min_lines: 80
    - path: "INTEGRATION.md"
      provides: "CLI 调用方式章节"
      contains: "## CLI 调用方式"
    - path: "INTEGRATION.md"
      provides: "JSON API 章节"
      contains: "## JSON API"
---

<objective>
创建 INTEGRATION.md 文档的基础结构,包含概述、CLI 调用方式和 JSON API 三个核心章节。文档应该简洁专业,面向有经验的开发者,重点说明 API 规范和参数说明。

Purpose: 为第三方开发者提供清晰、准确的 CLI API 集成指南,基于 Phase 8.1 已实现的 --json-help、--quiet、--json-output 功能。

Output: INTEGRATION.md 文件(项目根目录),包含完整的 CLI API 使用说明。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-集成文档/09-CONTEXT.md
@.planning/phases/09-集成文档/09-RESEARCH.md

# Phase 8.1 CLI API 实现
@.planning/phases/08.1-cli-api-enhancement/08.1-01-SUMMARY.md
@.planning/phases/08.1-cli-api-enhancement/08.1-02-SUMMARY.md
@.planning/phases/08.1-cli-api-enhancement/08.1-03-SUMMARY.md

# CLI 实现
@src/gallery_dl_auo/cli/main.py
@src/gallery_dl_auo/cli/json_help.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 INTEGRATION.md 基础结构</name>
  <files>INTEGRATION.md</files>
  <action>
创建 INTEGRATION.md 文件的基础结构和概述章节:

1. **文档标题和概述**:
   ```markdown
   # 集成指南

   本文档为第三方开发者提供 gallery-dl-auto 的集成指南,包括 CLI 调用方式、参数说明、示例代码和退出码参考。

   **适用场景**:
   - 自动化脚本集成
   - 第三方工具调用
   - CI/CD 流程集成
   - 批量下载任务编排

   **前置要求**:
   - Python 3.10+
   - 已安装 gallery-dl-auto (pip install gallery-dl-auto)
   - 基本熟悉 CLI 工具使用
   ```

2. **创建章节占位符** (后续任务填充):
   ```markdown
   ## CLI 调用方式
   [将在本计划后续任务中填充]

   ## JSON API
   [将在本计划后续任务中填充]

   ## 示例代码
   [将在计划 09-02 中填充]

   ## 退出码参考
   [将在计划 09-03 中填充]

   ## 最佳实践
   [将在计划 09-03 中填充]
   ```

**重要约束**:
- 文档必须放在项目根目录(与 README.md 平级)
- 使用中文撰写,保持简洁专业的风格
- 假设读者熟悉 CLI 工具集成,避免冗长的基础说明
  </action>
  <verify>
    <automated>test -f "INTEGRATION.md" &amp;&amp; grep -q "# 集成指南" "INTEGRATION.md" &amp;&amp; grep -q "## CLI 调用方式" "INTEGRATION.md" &amp;&amp; grep -q "## JSON API" "INTEGRATION.md"</automated>
    <manual>检查文档结构清晰,章节占位符完整</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>INTEGRATION.md 文件已创建,包含基础结构和章节占位符</done>
</task>

<task type="auto">
  <name>Task 2: 编写 CLI 调用方式章节</name>
  <files>INTEGRATION.md</files>
  <action>
编写 INTEGRATION.md 的"CLI 调用方式"章节,说明基本命令格式、全局参数和参数优先级:

1. **基本命令格式**:
   ```markdown
   ## CLI 调用方式

   ### 基本命令格式

   gallery-dl-auto 采用 Click CLI 框架,提供一致的命令行接口:

   ```bash
   pixiv-downloader [全局选项] <命令> [命令选项] [参数]
   ```

   **示例**:
   ```bash
   # 下载每日排行榜
   pixiv-downloader download --type daily --date 2026-02-25

   # 静默模式查询配置
   pixiv-downloader --quiet config get download_dir

   # JSON 输出获取版本信息
   pixiv-downloader --json-output version
   ```
   ```

2. **全局参数**:
   ```markdown
   ### 全局参数

   所有命令都支持以下全局参数:

   | 参数 | 简写 | 说明 | 默认值 |
   |------|------|------|--------|
   | --verbose | -v | 详细输出模式,显示进度和日志信息 | 关闭 |
   | --quiet | -q | 静默模式,禁用所有控制台输出(包括 stderr) | 关闭 |
   | --json-output | | JSON 输出模式,确保所有输出都是有效的 JSON 格式 | 关闭 |
   | --json-help | | 输出命令元数据的 JSON 格式帮助信息 | 关闭 |

   **参数说明**:
   - `--verbose`: 启用详细模式,显示下载进度、文件名等信息。适用于交互式使用。
   - `--quiet`: 启用静默模式,禁用所有控制台输出,仅保留文件日志。适用于后台任务和第三方工具调用。
   - `--json-output`: 启用 JSON 输出模式,确保所有输出(包括错误信息)都是有效的 JSON 格式。适用于第三方工具集成。
   - `--json-help`: 输出所有命令的结构化元数据,用于程序化发现 CLI API。不影响命令执行。
   ```

3. **参数优先级**:
   ```markdown
   ### 参数优先级

   当多个输出控制参数同时使用时,优先级为:

   **--json-output > --quiet > --verbose**

   **示例**:
   ```bash
   # 静默模式(verbose 被忽略)
   pixiv-downloader --quiet --verbose download --type daily

   # JSON 输出模式(quiet 和 verbose 被忽略)
   pixiv-downloader --json-output --quiet --verbose download --type daily

   # 详细模式(无冲突)
   pixiv-downloader --verbose download --type daily
   ```
   ```

**重要约束**:
- 基于实际实现的参数,不要虚构参数
- 所有示例命令必须可运行
- 使用表格提高信息密度
  </action>
  <verify>
    <automated>grep -q "### 基本命令格式" "INTEGRATION.md" &amp;&amp; grep -q "### 全局参数" "INTEGRATION.md" &amp;&amp; grep -q "### 参数优先级" "INTEGRATION.md" &amp;&amp; grep -q "json-output &gt; --quiet &gt; --verbose" "INTEGRATION.md"</automated>
    <manual>检查参数说明准确,优先级描述正确</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>CLI 调用方式章节已完成,包含基本命令格式、全局参数和参数优先级说明</done>
</task>

<task type="auto">
  <name>Task 3: 编写 JSON API 章节</name>
  <files>INTEGRATION.md</files>
  <action>
编写 INTEGRATION.md 的"JSON API"章节,说明 --json-help、--quiet、--json-output 的使用:

1. **JSON 帮助系统**:
   ```markdown
   ## JSON API

   ### 获取命令元数据 (--json-help)

   `--json-help` 参数输出所有命令的结构化元数据,用于程序化发现 CLI API:

   ```bash
   pixiv-downloader --json-help
   ```

   **输出格式**:
   ```json
   {
     "download": {
       "name": "download",
       "description": "Download Pixiv ranking images",
       "parameters": [
         {
           "name": "type",
           "type": "string",
           "required": true,
           "description": "Ranking type: daily, weekly, monthly",
           "default_value": null
         },
         ...
       ]
     },
     ...
   }
   ```

   **字段说明**:
   - `name`: 命令名称
   - `description`: 命令描述
   - `parameters`: 参数列表,每个参数包含:
     - `name`: 参数名称
     - `type`: 参数类型(string, integer, boolean, array)
     - `required`: 是否必需
     - `description`: 参数描述
     - `default_value`: 默认值(null 表示无默认值)

   **使用场景**:
   - 第三方工具自动发现可用命令
   - IDE 插件生成命令补全
   - 文档生成工具提取 API 信息
   ```

2. **静默模式**:
   ```markdown
   ### 静默模式 (--quiet)

   `--quiet` 参数禁用所有控制台输出,仅保留文件日志:

   ```bash
   pixiv-downloader --quiet download --type daily
   ```

   **行为特性**:
   - 禁用所有控制台输出(stdout 和 stderr)
   - 保留文件日志(用于调试)
   - 不保证 JSON 格式输出
   - 适用于后台任务和批处理

   **与 --json-output 的区别**:
   - `--quiet`: 仅静默输出,不保证 JSON 格式
   - `--json-output`: 确保输出为有效的 JSON 格式
   - 第三方工具集成推荐使用 `--json-output` 而非 `--quiet`
   ```

3. **JSON 输出模式**:
   ```markdown
   ### JSON 输出模式 (--json-output)

   `--json-output` 参数确保所有输出都是有效的 JSON 格式:

   ```bash
   pixiv-downloader --json-output download --type daily
   ```

   **行为特性**:
   - 所有输出(stdout 和 stderr)都是有效的 JSON 格式
   - 错误信息也以 JSON 格式返回
   - 适用于第三方工具集成和自动化流程
   - 可靠的 JSON 解析保证

   **适用场景**:
   - 第三方工具集成
   - CI/CD 流程集成
   - 批量任务编排
   - 结果解析和监控

   **注意**: 当前版本命令尚未完全实现 JSON 输出逻辑,建议使用 `--quiet` + 文件日志方式进行集成。
   ```

**重要约束**:
- 基于Phase 8.1 实际实现的功能编写
- 包含真实的输出格式示例(基于 --json-help 实际输出)
- 说明当前限制(命令尚未完全实现 JSON 输出)
  </action>
  <verify>
    <automated>grep -q "### 获取命令元数据 (--json-help)" "INTEGRATION.md" &amp;&amp; grep -q "### 静默模式 (--quiet)" "INTEGRATION.md" &amp;&amp; grep -q "### JSON 输出模式 (--json-output)" "INTEGRATION.md"</automated>
    <manual>检查 JSON API 说明准确,包含真实的输出格式示例</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>JSON API 章节已完成,包含 --json-help、--quiet、--json-output 的完整说明</done>
</task>

</tasks>

<verification>
1. INTEGRATION.md 文件存在
2. 文档包含完整的概述章节
3. 文档包含完整的 CLI 调用方式章节(基本命令格式、全局参数、参数优先级)
4. 文档包含完整的 JSON API 章节(--json-help, --quiet, --json-output)
5. 所有参数说明基于实际实现,优先级描述正确
6. 文档使用中文撰写,简洁专业
</verification>

<success_criteria>
1. INTEGRATION.md 文件已创建在项目根目录
2. CLI 调用方式章节完整准确(基于 Phase 8.1 实现)
3. JSON API 章节完整准确(基于 Phase 8.1 实现)
4. 文档格式规范,信息密度高
5. DOCS-01 需求部分满足(调用方式、参数说明已文档化)
</success_criteria>

<output>
After completion, create `.planning/phases/09-集成文档/09-01-SUMMARY.md`
</output>
