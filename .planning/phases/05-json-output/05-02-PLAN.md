---
phase: 05-json-output
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/gallery_dl_auo/cli/download_cmd.py
  - src/gallery_dl_auo/cli/refresh_cmd.py
  - src/gallery_dl_auo/download/ranking_downloader.py
autonomous: true
requirements:
  - OUTP-01
  - OUTP-02
  - OUTP-03
  - OUTP-04

must_haves:
  truths:
    - "download 命令输出包含标准化的 success/data/error 结构"
    - "refresh 命令输出包含标准化的 success/data/error 结构"
    - "所有错误包含错误码、可读消息和可选详情"
    - "JSON 输出到 stdout,日志信息输出到 stderr"
  artifacts:
    - path: "src/gallery_dl_auo/cli/download_cmd.py"
      provides: "标准化 download 命令"
      contains: "from gallery_dl_auo.models.output import DownloadOutput"
    - path: "src/gallery_dl_auo/cli/refresh_cmd.py"
      provides: "标准化 refresh 命令"
      contains: "from gallery_dl_auo.models.output import RefreshOutput"
  key_links:
    - from: "download_cmd.py"
      to: "models/output.py"
      via: "import DownloadOutput"
      pattern: "from gallery_dl_auo.models.output import"
    - from: "download_cmd.py"
      to: "utils/error_codes.py"
      via: "import ErrorCode"
      pattern: "from gallery_dl_auo.utils.error_codes import"
---

<objective>
更新 download 和 refresh 命令使用标准化 JSON 输出模型,确保所有错误都包含错误码,实现 stdout/stderr 分离。

Purpose: 让第三方程序能够可靠地解析 CLI 输出,根据错误码进行程序化处理,日志信息不污染 JSON 流。

Output: 更新后的 download_cmd.py 和 refresh_cmd.py,使用 05-01 定义的输出模型
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-json-output/05-RESEARCH.md
@.planning/phases/05-json-output/05-01-SUMMARY.md
@src/gallery_dl_auo/cli/download_cmd.py
@src/gallery_dl_auo/cli/refresh_cmd.py
@src/gallery_dl_auo/download/ranking_downloader.py
@src/gallery_dl_auo/models/output.py
</context>

<tasks>

<task type="auto">
  <name>扩展输出模型支持 refresh 命令</name>
  <files>src/gallery_dl_auo/models/output.py</files>
  <action>
在 output.py 中添加 refresh 命令的输出模型 (追加到现有文件):

```python
class RefreshSuccessData(BaseModel):
    """refresh 命令成功时的数据结构"""
    old_token_masked: str = Field(..., description="旧 token (遮蔽)")
    new_token_masked: str = Field(..., description="新 token (遮蔽)")
    expires_in_days: int = Field(..., description="token 有效期 (天)")

class RefreshOutput(BaseModel):
    """refresh 命令的输出结构"""
    success: bool
    data: RefreshSuccessData | None = None
    error: ErrorDetail | None = None

    def to_json(self) -> str:
        """序列化为 JSON 字符串"""
        return self.model_dump_json(
            exclude_none=True,
            indent=2,
            ensure_ascii=False
        )
```

**关键决策**:
- RefreshSuccessData 包含与当前 refresh 命令输出相同的字段 (保持向后兼容)
- 使用相同的 `to_json()` 方法确保一致性
- token 使用遮蔽版本 (隐私保护)

**参考现有代码**:
- refresh_cmd.py 当前输出: `{"old_token": "xxxxx...", "new_token": "yyyyy...", "expires_in_days": 30}`
- 使用相同的字段名 (old_token_masked, new_token_masked 更明确)

**不要做**:
- 不要修改 DownloadOutput 模型 (已在 05-01 定义)
- 不要添加额外的字段 (保持向后兼容)
  </action>
  <verify>
<automated>python -c "from gallery_dl_auo.models.output import RefreshOutput, RefreshSuccessData; print('Refresh models imported successfully')"</automated>
<manual>检查 RefreshOutput 结构是否与当前 refresh 命令输出兼容</manual>
  </verify>
  <done>
- [ ] output.py 包含 RefreshSuccessData 和 RefreshOutput 模型
- [ ] 模型能够成功导入
- [ ] 字段名与当前 refresh 命令输出保持一致
  </done>
</task>

<task type="auto">
  <name>更新 download 命令使用标准化输出</name>
  <files>src/gallery_dl_auo/cli/download_cmd.py</files>
  <action>
重构 download_cmd.py 使用标准化输出模型:

1. **导入新模型**:
   ```python
   from gallery_dl_auo.models.output import (
       DownloadOutput,
       DownloadSuccessData,
       ErrorDetail,
   )
   from gallery_dl_auo.utils.error_codes import ErrorCode
   ```

2. **重构 token 检查逻辑**:
   ```python
   if not token_data or not token_data.get("refresh_token"):
       error_output = DownloadOutput(
           success=False,
           error=ErrorDetail(
               code=ErrorCode.AUTH_TOKEN_NOT_FOUND,
               message="No token found. Run 'pixiv-downloader login' first.",
               details=None
           )
       )
       print(error_output.to_json())
       sys.exit(1)
   ```

3. **重构 API 客户端初始化逻辑**:
   ```python
   try:
       client = PixivClient(refresh_token=token_data["refresh_token"])
   except Exception as e:
       error_output = DownloadOutput(
           success=False,
           error=ErrorDetail(
               code=ErrorCode.AUTH_TOKEN_INVALID,
               message=f"Authentication failed: {e}",
               details={"exception_type": type(e).__name__}
           )
       )
       print(error_output.to_json())
       sys.exit(1)
   ```

4. **重构成功输出逻辑**:
   ```python
   success_data = DownloadSuccessData(
       total=results["total"],
       success_count=len(results["success"]),
       failed_count=len(results["failed"]),
       output_dir=str(output_dir),
       date=date or datetime.date.today().strftime("%Y-%m-%d"),
       mode=mode,
       path_template=path_template,
       success_list=results["success"],
       failed_list=results["failed"]
   )

   output = DownloadOutput(success=True, data=success_data)
   print(output.to_json())

   # 5. Return exit code 1 if any failures
   if results["failed"]:
       sys.exit(1)
   ```

5. **添加 API 错误处理** (如果 ranking_downloader 返回 error):
   ```python
   if "error" in results:
       # API 级别错误 (如网络错误、服务器错误)
       error_output = DownloadOutput(
           success=False,
           error=ErrorDetail(
               code=ErrorCode.API_SERVER_ERROR,
               message=results["error"],
               details=None
           )
       )
       print(error_output.to_json())
       sys.exit(1)
   ```

**关键决策**:
- 所有错误都使用 ErrorDetail 模型,包含错误码
- 使用 `to_json()` 方法序列化,确保格式一致
- 保持 `ensure_ascii=False` (已在 to_json() 中配置)
- 保持退出码策略: 成功/部分失败=0 (如果有失败则返回 1), 完全失败=1

**参考现有代码**:
- 当前 download_cmd.py 使用 `print(json.dumps(...))` (保持相同的输出位置)
- 当前错误消息保持一致 (如 "No token found. Run 'pixiv-downloader login' first.")

**不要做**:
- 不要将日志信息输出到 stdout (仅 JSON 输出到 stdout)
- 不要改变退出码策略 (让第三方程序决定重试策略)
- 不要移除 sys.exit(1) 调用 (保持错误信号)
  </action>
  <verify>
<automated>cd tmp && pixiv-downloader download --help 2>&1 | head -5</automated>
<manual>手动测试: 运行 download 命令,检查 JSON 输出格式是否符合标准</manual>
<sampling_rate>在任务完成后立即验证,确保命令仍然可用</sampling_rate>
  </verify>
  <done>
- [ ] download_cmd.py 导入标准化输出模型
- [ ] 所有错误使用 ErrorDetail 模型
- [ ] 成功输出使用 DownloadSuccessData 模型
- [ ] JSON 输出包含 success/data 或 success/error 结构
- [ ] 命令仍然可用 (--help 正常)
  </done>
</task>

<task type="auto">
  <name>更新 refresh 命令使用标准化输出</name>
  <files>src/gallery_dl_auo/cli/refresh_cmd.py</files>
  <action>
重构 refresh_cmd.py 使用标准化输出模型:

1. **导入新模型**:
   ```python
   from gallery_dl_auo.models.output import (
       RefreshOutput,
       RefreshSuccessData,
       ErrorDetail,
   )
   from gallery_dl_auo.utils.error_codes import ErrorCode
   ```

2. **重构 token 加载逻辑**:
   ```python
   storage = get_default_token_storage()
   token_data = storage.load_token()

   if not token_data or not token_data.get("refresh_token"):
       error_output = RefreshOutput(
           success=False,
           error=ErrorDetail(
               code=ErrorCode.AUTH_TOKEN_NOT_FOUND,
               message="No token found. Run 'pixiv-downloader login' first.",
               details=None
           )
       )
       print(error_output.to_json())
       sys.exit(1)
   ```

3. **重构 token 刷新逻辑**:
   ```python
   try:
       oauth = PixivOAuth()
       new_token_data = oauth.refresh_tokens(token_data["refresh_token"])
       storage.save_token(new_token_data)
   except OAuthError as e:
       error_output = RefreshOutput(
           success=False,
           error=ErrorDetail(
               code=ErrorCode.AUTH_REFRESH_FAILED,
               message=f"Token refresh failed: {e}",
               details={"exception_type": type(e).__name__}
           )
       )
       print(error_output.to_json())
       sys.exit(1)
   except Exception as e:
       error_output = RefreshOutput(
           success=False,
           error=ErrorDetail(
               code=ErrorCode.INTERNAL_ERROR,
               message=f"Unexpected error: {e}",
               details={"exception_type": type(e).__name__}
           )
       )
       print(error_output.to_json())
       sys.exit(1)
   ```

4. **重构成功输出逻辑**:
   ```python
   success_data = RefreshSuccessData(
       old_token_masked=mask_token(token_data["refresh_token"]),
       new_token_masked=mask_token(new_token_data["refresh_token"]),
       expires_in_days=30  # Pixiv token 有效期约 30 天
   )

   output = RefreshOutput(success=True, data=success_data)
   print(output.to_json())
   ```

**关键决策**:
- 保持 mask_token() 函数不变 (隐私保护)
- 使用 RefreshOutput 模型确保一致性
- 区分 OAuthError (预期错误) 和 Exception (未预期错误)
- 保持相同的错误码 (AUTH_TOKEN_NOT_FOUND, AUTH_REFRESH_FAILED, INTERNAL_ERROR)

**参考现有代码**:
- 当前 refresh_cmd.py 输出结构 (保持字段名一致)
- 当前错误处理逻辑 (捕获 OAuthError 和 Exception)

**不要做**:
- 不要改变 token 遮蔽策略 (10+10 字符)
- 不要移除 mask_token() 函数
- 不要改变退出码策略
  </action>
  <verify>
<automated>cd tmp && pixiv-downloader refresh --help 2>&1 | head -5</automated>
<manual>手动测试: 运行 refresh 命令 (如果没有 token 会返回错误),检查 JSON 输出格式</manual>
<sampling_rate>在任务完成后立即验证,确保命令仍然可用</sampling_rate>
  </verify>
  <done>
- [ ] refresh_cmd.py 导入标准化输出模型
- [ ] 所有错误使用 ErrorDetail 模型
- [ ] 成功输出使用 RefreshSuccessData 模型
- [ ] JSON 输出包含 success/data 或 success/error 结构
- [ ] 命令仍然可用 (--help 正常)
  </done>
</task>

<task type="auto">
  <name>确保日志输出到 stderr (可选优化)</name>
  <files>src/gallery_dl_auo/cli/download_cmd.py, src/gallery_dl_auo/cli/refresh_cmd.py</files>
  <action>
检查并确保所有日志信息输出到 stderr 而非 stdout:

1. **检查 download_cmd.py**:
   - 如果使用 `logger.info()` 或 `logger.debug()`,确认 logger 配置输出到 stderr
   - 如果使用 `print()` 输出进度信息,改为 `print(..., file=sys.stderr)`

2. **检查 refresh_cmd.py**:
   - 如果使用 `logger.info()` 或 `logger.debug()`,确认 logger 配置输出到 stderr

3. **验证 logger 配置**:
   - 检查 `src/gallery_dl_auo/__init__.py` 中的 logger 配置
   - 确保 RichHandler 或 StreamHandler 输出到 stderr

**注意**: 如果当前代码没有显式的日志输出,可以跳过此任务。

**参考 RESEARCH.md "Pattern 4: stdout/stderr 分离"**:
- JSON 输出 → stdout (可以被管道捕获)
- 日志、进度 → stderr (不污染 JSON 流)

**不要做**:
- 不要添加新的日志输出 (仅确保现有的输出到 stderr)
- 不要移除必要的日志信息
  </action>
  <verify>
<automated>python -c "import logging; import sys; logger = logging.getLogger('gallery_dl_auo'); print('Logger check passed')" 2>&1</automated>
<manual>手动测试: 运行命令并重定向 stdout,检查日志是否仍然可见 (应该输出到 stderr)</manual>
  </verify>
  <done>
- [ ] 确认 logger 配置输出到 stderr
- [ ] 确认 print() 仅用于 JSON 输出 (stdout)
- [ ] 确认日志信息不污染 JSON 输出
  </done>
</task>

</tasks>

<verification>
**阶段验证** (在所有任务完成后执行):

1. **download 命令验证**:
   ```bash
   # 测试成功场景 (假设有 token)
   pixiv-downloader download --date 2026-02-24 --output ./tmp/test-output 2>/dev/null | jq '.success'

   # 测试错误场景 (假设没有 token)
   pixiv-downloader logout  # 移除 token
   pixiv-downloader download --date 2026-02-24 2>/dev/null | jq '.error.code'
   # 期望输出: "AUTH_TOKEN_NOT_FOUND"
   ```

2. **refresh 命令验证**:
   ```bash
   # 测试错误场景 (假设没有 token)
   pixiv-downloader logout
   pixiv-downloader refresh 2>/dev/null | jq '.error.code'
   # 期望输出: "AUTH_TOKEN_NOT_FOUND"
   ```

3. **JSON 格式验证**:
   - 成功输出包含 `success: true` 和 `data` 字段
   - 错误输出包含 `success: false` 和 `error` 字段 (包含 `code`, `message`, `details`)
   - 中文字符不被转义为 `\uXXXX`
   - 日志信息不出现在 stdout (仅在 stderr)
</verification>

<success_criteria>
**必须达成的目标**:

1. **download 命令标准化**:
   - [x] 使用 DownloadOutput 模型
   - [x] 所有错误包含 ErrorCode
   - [x] 成功输出包含 DownloadSuccessData
   - [x] 命令仍然可用

2. **refresh 命令标准化**:
   - [x] 使用 RefreshOutput 模型
   - [x] 所有错误包含 ErrorCode
   - [x] 成功输出包含 RefreshSuccessData
   - [x] 命令仍然可用

3. **输出一致性**:
   - [x] 所有命令使用 `to_json()` 方法
   - [x] `exclude_none=True` 移除 null 字段
   - [x] `ensure_ascii=False` 支持 Unicode
   - [x] `indent=2` 美化输出

4. **stdout/stderr 分离**:
   - [x] JSON 输出到 stdout
   - [x] 日志信息输出到 stderr (如果有)
</success_criteria>

<output>
完成后创建 `.planning/phases/05-json-output/05-02-SUMMARY.md`
</output>
