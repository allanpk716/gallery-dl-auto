---
phase: 01-项目基础
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - tests/conftest.py
  - tests/test_cli/test_main.py
  - tests/test_cli/test_config_cmd.py
  - tests/test_config/test_loader.py
  - tests/test_config/test_schema.py
  - .pre-commit-config.yaml
  - README.md
autonomous: true
requirements:
  - OUTP-05
  - OUTP-06

must_haves:
  truths:
    - "所有测试通过(pytest)"
    - "代码质量检查通过(black, ruff, mypy)"
    - "pre-commit 钩子正确配置"
    - "README.md 包含项目介绍、安装和快速开始"
    - "用户能够通过 README 快速了解项目和如何使用"
  artifacts:
    - path: "tests/conftest.py"
      provides: "pytest 共享 fixture"
      contains: "@pytest.fixture"
      min_lines: 20
    - path: "tests/test_cli/test_main.py"
      provides: "CLI 主命令测试"
      contains: "def test_cli_help"
      min_lines: 30
    - path: "tests/test_config/test_loader.py"
      provides: "配置加载测试"
      contains: "def test_load_config"
      min_lines: 30
    - path: ".pre-commit-config.yaml"
      provides: "pre-commit 钩子配置"
      contains: "repos:"
      min_lines: 40
    - path: "README.md"
      provides: "项目文档"
      contains: "## 安装"
      min_lines: 80
  key_links:
    - from: "tests/test_cli/test_main.py"
      to: "CliRunner"
      via: "runner.invoke"
      pattern: "CliRunner\\(\\)"
    - from: ".pre-commit-config.yaml"
      to: "black, ruff, mypy"
      via: "hooks"
      pattern: "- id: (black|ruff|mypy)"
    - from: "README.md"
      to: "CLI 命令"
      via: "快速开始示例"
      pattern: "pixiv-downloader"
---

<objective>
建立完整的测试框架、代码质量工具链和项目文档,确保代码质量和可维护性。

Purpose: 提供自动化测试保障、代码质量检查和清晰的项目文档,为后续开发奠定基础。
Output: 测试套件、pre-commit 配置、README 文档。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-项目基础/01-CONTEXT.md
@.planning/phases/01-项目基础/01-RESEARCH.md
@.planning/phases/01-项目基础/01-01-SUMMARY.md
@.planning/phases/01-项目基础/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试框架和 CLI 测试</name>
  <files>
    tests/conftest.py
    tests/test_cli/__init__.py
    tests/test_cli/test_main.py
    tests/test_cli/test_config_cmd.py
  </files>
  <action>
创建 pytest 测试框架和 CLI 测试套件。

**1. tests/conftest.py - 共享 fixture**

```python
import pytest
from click.testing import CliRunner
from omegaconf import OmegaConf

from gallery_dl_auo.config.schema import AppConfig


@pytest.fixture
def runner():
    """提供 CliRunner fixture"""
    return CliRunner()


@pytest.fixture
def sample_config():
    """提供示例配置"""
    return OmegaConf.structured(AppConfig())
```

**2. tests/test_cli/__init__.py**

空文件,标记测试包。

**3. tests/test_cli/test_main.py - 主命令测试**

```python
import pytest
from click.testing import CliRunner

from gallery_dl_auo.cli.main import cli


def test_cli_help(runner: CliRunner):
    """测试 --help 选项"""
    result = runner.invoke(cli, ["--help"])
    assert result.exit_code == 0
    assert "Pixiv 排行榜下载器" in result.output


def test_cli_verbose_option(runner: CliRunner):
    """测试 --verbose 选项"""
    result = runner.invoke(cli, ["--verbose", "version"])
    assert result.exit_code == 0


def test_cli_quiet_option(runner: CliRunner):
    """测试 --quiet 选项"""
    result = runner.invoke(cli, ["--quiet", "version"])
    assert result.exit_code == 0


def test_version_command(runner: CliRunner):
    """测试 version 命令"""
    result = runner.invoke(cli, ["version"])
    assert result.exit_code == 0
    assert "version" in result.output
```

**4. tests/test_cli/test_config_cmd.py - config 命令测试**

```python
import pytest
from click.testing import CliRunner
from gallery_dl_auo.cli.main import cli


def test_config_command_with_file(runner: CliRunner, tmp_path):
    """测试 config 命令(有配置文件)"""
    # 创建临时配置文件
    config_file = tmp_path / "config.yaml"
    config_file.write_text("save_path: ./downloads\nlog_level: INFO\n")

    # 切换到临时目录
    import os
    old_cwd = os.getcwd()
    try:
        os.chdir(tmp_path)
        result = runner.invoke(cli, ["config"])
        assert result.exit_code == 0
        assert "配置" in result.output
    finally:
        os.chdir(old_cwd)


def test_config_command_without_file(runner: CliRunner):
    """测试 config 命令(无配置文件)"""
    result = runner.invoke(cli, ["config"])
    # 应该失败或提示文件不存在
    assert result.exit_code != 0 or "错误" in result.output
```

**关键点**:
- 使用 CliRunner 隔离测试环境
- fixture 提供可复用的测试组件
- 测试正常路径和错误路径
- tmp_path fixture 提供临时目录

**避免**:
- 不要在测试中修改全局状态(使用 tmp_path)
- 不要跳过错误情况测试
  </action>
  <verify>
    <automated>
pytest tests/test_cli/ -v
    </automated>
    <manual>检查测试覆盖率,确保主要功能都有测试</manual>
    <sampling_rate>commit 后立即验证</sampling_rate>
  </verify>
  <done>
pytest 测试框架就绪。
所有 CLI 测试通过。
测试覆盖率 >= 50%(Phase 1 目标)。
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建配置测试</name>
  <files>
    tests/test_config/__init__.py
    tests/test_config/test_schema.py
    tests/test_config/test_loader.py
  </files>
  <action>
创建配置系统的测试套件。

**1. tests/test_config/__init__.py**

空文件,标记测试包。

**2. tests/test_config/test_schema.py - schema 测试**

```python
import pytest
from gallery_dl_auo.config.schema import AppConfig


def test_app_config_defaults():
    """测试配置默认值"""
    config = AppConfig()
    assert config.save_path == "./downloads"
    assert config.concurrent_downloads == 3
    assert config.request_interval == 1.0
    assert config.log_level == "INFO"


def test_app_config_custom_values():
    """测试自定义配置值"""
    config = AppConfig(
        save_path="/custom/path",
        concurrent_downloads=5,
        log_level="DEBUG"
    )
    assert config.save_path == "/custom/path"
    assert config.concurrent_downloads == 5
    assert config.log_level == "DEBUG"
```

**3. tests/test_config/test_loader.py - loader 测试**

```python
import pytest
from omegaconf import OmegaConf, DictConfig

from gallery_dl_auo.config.schema import AppConfig
from gallery_dl_auo.config.loader import load_and_validate_config


def test_load_valid_config(sample_config: DictConfig):
    """测试加载有效配置"""
    config = load_and_validate_config(sample_config)
    assert isinstance(config, AppConfig)
    assert config.concurrent_downloads == 3


def test_validate_concurrent_downloads_too_low(sample_config: DictConfig):
    """测试并发数过低验证"""
    sample_config.concurrent_downloads = 0
    with pytest.raises(ValueError) as exc_info:
        load_and_validate_config(sample_config)
    assert "concurrent_downloads" in str(exc_info.value)


def test_validate_request_interval_too_low(sample_config: DictConfig):
    """测试请求间隔过低验证"""
    sample_config.request_interval = 0.1
    with pytest.raises(ValueError) as exc_info:
        load_and_validate_config(sample_config)
    assert "request_interval" in str(exc_info.value)


def test_validate_invalid_log_level(sample_config: DictConfig):
    """测试无效日志级别验证"""
    sample_config.log_level = "INVALID"
    with pytest.raises(ValueError) as exc_info:
        load_and_validate_config(sample_config)
    assert "log_level" in str(exc_info.value)
```

**关键点**:
- 测试默认值和自定义值
- 测试验证逻辑(边界值、无效值)
- 使用 pytest.raises 测试异常
- 错误信息应该清晰易懂

**避免**:
- 不要只测试正常路径(必须测试错误处理)
- 不要使用硬编码的配置值(使用 fixture)
  </action>
  <verify>
    <automated>
pytest tests/test_config/ -v
    </automated>
    <manual>检查配置测试覆盖率,确保所有验证逻辑都有测试</manual>
    <sampling_rate>commit 后立即验证</sampling_rate>
  </verify>
  <done>
所有配置测试通过。
验证逻辑被充分测试(正常和异常情况)。
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 pre-commit 配置和 README</name>
  <files>
    .pre-commit-config.yaml
    README.md
  </files>
  <action>
创建 pre-commit 钩子配置和项目文档。

**1. .pre-commit-config.yaml - pre-commit 配置**

```yaml
repos:
  # 通用检查
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--unsafe]
      - id: check-added-large-files
      - id: check-merge-conflict

  # Python 代码格式化
  - repo: https://github.com/psf/black
    rev: 24.2.0
    hooks:
      - id: black
        language_version: python3.10

  # Ruff linting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.2.1
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]

  # Mypy 类型检查
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.8.0
    hooks:
      - id: mypy
        additional_dependencies:
          - types-PyYAML
        args: [--strict]
```

**2. README.md - 项目文档**

```markdown
# Pixiv 排行榜下载器 (gallery-dl-auto)

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

自动化获取 pixiv refresh token 并下载排行榜内容 — 用户首次手动登录后,程序自动捕获、存储和更新 refresh token,无需手动从浏览器开发者工具中复制,实现真正的自动化下载流程。

## 核心特性

- ✨ **自动化 Token 管理**: 首次登录后自动捕获和刷新 token
- 📥 **排行榜下载**: 支持日榜、周榜、月榜
- 📊 **完整元数据**: 获取作品标题、作者、标签、统计数据
- 🎯 **CLI 优先**: 命令行工具,易于集成和自动化
- 📦 **JSON 输出**: 结构化输出,支持程序化调用
- 🔧 **灵活配置**: YAML 配置文件 + CLI 参数覆盖

## 快速开始

### 安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/gallery-dl-auto.git
cd gallery-dl-auto

# 安装依赖
pip install -e .
```

### 基本使用

```bash
# 查看帮助
pixiv-downloader --help

# 查看版本
pixiv-downloader version

# 查看当前配置
pixiv-downloader config

# 诊断环境
pixiv-downloader doctor
```

## 配置

程序从当前目录的 `config.yaml` 加载配置。首次运行会使用默认配置。

### 配置文件示例

```yaml
# 下载配置
save_path: ./downloads        # 图片保存路径
concurrent_downloads: 3       # 并发下载数
request_interval: 1.0         # 请求间隔(秒)

# 日志配置
log_level: INFO               # 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL

# 网络配置
api_timeout: 30               # API 超时(秒)
max_retries: 3                # 重试次数
```

### 配置优先级

命令行参数 > 环境变量 > 配置文件 > 默认值

## 开发

### 安装开发依赖

```bash
pip install -e ".[dev]"
```

### 运行测试

```bash
pytest tests/ -v
```

### 代码质量检查

```bash
# 格式化代码
black src/ tests/

# Lint 检查
ruff check src/ tests/

# 类型检查
mypy src/
```

### Pre-commit 钩子

```bash
# 安装 pre-commit 钩子
pre-commit install

# 手动运行所有检查
pre-commit run --all-files
```

## 路线图

- [x] Phase 1: 项目基础 ✅
- [ ] Phase 2: Token 自动化
- [ ] Phase 3: 排行榜基础下载
- [ ] Phase 4: 内容与元数据
- [ ] Phase 5: JSON 输出
- [ ] Phase 6: 多排行榜支持
- [ ] Phase 7: 错误处理与健壮性
- [ ] Phase 8: 用户体验优化

## 许可证

[MIT License](LICENSE)

## 贡献

欢迎贡献!请查看 [CONTRIBUTING.md](CONTRIBUTING.md) 了解详情。

## 致谢

- [Click](https://click.palletsprojects.com/) - CLI 框架
- [Hydra](https://hydra.cc/) - 配置管理
- [Rich](https://github.com/Textualize/rich) - 终端美化
```

**关键点**:
- README 包含项目简介、安装、使用、配置、开发指南
- 使用徽章(badges)显示项目状态
- 提供清晰的快速开始示例
- pre-commit 配置包含 black, ruff, mypy
- 使用 check-yaml --unsafe 避免 load() 警告

**避免**:
- 不要写过长的 README(保持在合理长度)
- 不要遗漏开发指南(帮助贡献者)
  </action>
  <verify>
    <automated>
pre-commit run --all-files --config .pre-commit-config.yaml 2>&1 | head -20
    </automated>
    <manual>检查 README.md 包含所有必需章节: 简介、安装、快速开始、配置、开发</manual>
    <sampling_rate>commit 后立即验证</sampling_rate>
  </verify>
  <done>
pre-commit 钩子配置完成。
README.md 包含完整的项目文档。
可以运行 `pre-commit run --all-files` 检查代码质量。
  </done>
</task>

</tasks>

<verification>
**Phase 1 Plan 03 验证清单:**

1. **测试框架验证**:
   - [ ] pytest 测试套件存在
   - [ ] 所有测试通过(pytest tests/ -v)
   - [ ] 测试覆盖率 >= 50%

2. **代码质量验证**:
   - [ ] pre-commit 配置存在
   - [ ] pre-commit run --all-files 通过
   - [ ] black 格式化代码
   - [ ] ruff 检查通过
   - [ ] mypy 类型检查通过

3. **文档验证**:
   - [ ] README.md 存在并包含所有章节
   - [ ] 快速开始示例可运行
   - [ ] 配置说明清晰
</verification>

<success_criteria>
**Plan 03 成功标准:**

1. **可观察的真实情况**:
   - 运行 `pytest tests/ -v` 所有测试通过
   - 运行 `pre-commit run --all-files` 所有检查通过
   - README.md 包含项目介绍、安装、使用、配置、开发指南
   - 新用户能够通过 README 快速上手

2. **工件验证**:
   - tests/ 目录包含完整的测试套件
   - .pre-commit-config.yaml 包含 black, ruff, mypy 钩子
   - README.md 长度适中(~150 行)

3. **关键链接验证**:
   - tests/test_cli/test_main.py → CliRunner: runner.invoke 使用正确
   - .pre-commit-config.yaml → black/ruff/mypy: hooks 配置存在
   - README.md → CLI 命令: 快速开始示例包含 pixiv-downloader 命令
</success_criteria>

<output>
完成后创建 `.planning/phases/01-项目基础/01-03-SUMMARY.md` 记录:
- 测试框架结构
- 测试覆盖率和质量
- pre-commit 配置说明
- README 文档结构
- 遇到的问题和解决方案
</output>
