---
phase: 08.1-cli-api-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gallery_dl_auo/cli/main.py
  - src/gallery_dl_auo/utils/logging.py
  - tests/cli/test_json_output.py
  - tests/cli/conftest.py
autonomous: true
requirements:
  - CLI-02
  - CLI-03

must_haves:
  truths:
    - "用户使用 --quiet 参数时看不到任何进度和日志输出,仅收到最终 JSON 结果"
    - "用户使用 --json-output 参数时所有输出都是有效 JSON,包括错误信息"
    - "参数优先级正确: --json-output > --quiet > --verbose"
    - "--json-output 模式下的异常被捕获并转换为 JSON 错误响应"
  artifacts:
    - path: "src/gallery_dl_auo/cli/main.py"
      provides: "主命令组,添加 --quiet 和 --json-output 全局参数"
      contains: ["--quiet", "--json-output"]
      min_lines: 70
    - path: "src/gallery_dl_auo/utils/logging.py"
      provides: "日志系统,支持静默模式"
      contains: "quiet parameter"
    - path: "tests/cli/test_json_output.py"
      provides: "静默和 JSON 输出模式的测试"
      exports: ["test_quiet_mode", "test_json_output_format", "test_json_error_handling"]
  key_links:
    - from: "src/gallery_dl_auo/cli/main.py"
      to: "setup_logging"
      via: "传递 quiet 标志"
      pattern: "setup_logging.*quiet"
    - from: "src/gallery_dl_auo/cli/main.py"
      to: "ctx.obj"
      via: "存储输出模式状态"
      pattern: "ctx\\.obj\\[.*output_mode"
---

<objective>
实现 --quiet 和 --json-output 全局参数,控制 CLI 输出模式,确保第三方工具调用时获得干净的 JSON 输出。

Purpose: 第三方工具需要静默模式和统一的 JSON 输出,避免进度信息和日志污染输出流,确保错误信息也能被 JSON 解析器处理。

Output: 主命令组添加两个全局参数,日志系统支持静默模式,完整的测试覆盖。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-cli-api-enhancement/08.1-CONTEXT.md
@.planning/phases/08.1-cli-api-enhancement/08.1-RESEARCH.md

@src/gallery_dl_auo/cli/main.py
@src/gallery_dl_auo/utils/logging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展日志系统支持静默模式</name>
  <files>src/gallery_dl_auo/utils/logging.py</files>
  <action>
修改 setup_logging 函数,添加 quiet 参数以支持静默模式。

实现要点(遵循 CONTEXT.md 锁定决策):

1. **函数签名**: 修改为 `setup_logging(log_level="INFO", verbose=False, quiet=False)`

2. **静默模式行为**: 当 quiet=True 时:
   - **不添加任何控制台 handler**(包括 RichHandler)
   - **保留文件 handler**(StructuredFileHandler,用于调试)
   - 确保日志级别设置不影响文件日志

3. **参数优先级逻辑**:
   ```python
   # 详细模式仅在非静默模式下生效
   if verbose and not quiet:
       # 添加 RichHandler
   ```

4. **文档更新**: 更新函数 docstring 说明 quiet 参数:
   ```python
   """
   Args:
       log_level: 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
       verbose: 是否启用详细模式(控制台输出)
       quiet: 是否启用静默模式(禁用所有控制台输出)

   日志策略:
       - 详细模式(verbose=True, quiet=False): 控制台 INFO+, 文件 DEBUG+
       - 静默模式(quiet=True): 控制台无输出, 文件 DEBUG+
       - JSON 模式(外部传入 quiet=True): 控制台无输出, 文件 DEBUG+
   """
   ```

5. **兼容性**: 确保现有调用 `setup_logging(log_level, verbose)` 不受影响(quiet 默认 False)

注意事项:
- 不要修改 StructuredFileHandler 的逻辑
- 不要删除现有的 verbose 模式功能
- 静默模式应完全禁用控制台输出(包括 stderr)
  </action>
  <verify>
<verify>
  <automated>MISSING — Wave 0 must create tests/cli/test_json_output.py first</automated>
  <manual>手动验证: pixiv-downloader --quiet version 2>&1 | wc -l 应为 1(仅 JSON 输出)</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
  </verify>
  <done>
setup_logging 函数签名包含 quiet 参数,quiet=True 时控制台无任何输出(包括 stderr),文件日志正常记录。
  </done>
</task>

<task type="auto">
  <name>Task 2: 添加 --quiet 和 --json-output 全局参数</name>
  <files>src/gallery_dl_auo/cli/main.py</files>
  <action>
修改主命令组,添加 --quiet 和 --json-output 全局参数,实现参数优先级逻辑。

实现要点(遵循 CONTEXT.md 锁定决策):

1. **参数定义**: 在 `@click.group()` 装饰器下添加:
   ```python
   @click.option("--quiet", "-q", is_flag=True, help="静默模式:禁用所有输出")
   @click.option("--json-output", is_flag=True, help="JSON 输出模式:所有输出为 JSON 格式")
   ```

2. **函数签名**: 修改 cli 函数:
   ```python
   def cli(ctx: click.Context, verbose: bool, quiet: bool, json_output: bool) -> None:
   ```

3. **参数优先级逻辑**(锁定决策: --json-output > --quiet > --verbose):
   ```python
   ctx.ensure_object(dict)

   # 确定输出模式
   if json_output:
       ctx.obj["output_mode"] = "json"
   elif quiet:
       ctx.obj["output_mode"] = "quiet"
   else:
       ctx.obj["output_mode"] = "normal"

   # verbose 仅在 normal 模式下生效
   effective_verbose = verbose and ctx.obj["output_mode"] == "normal"

   # 配置日志系统
   setup_logging(
       log_level="DEBUG" if effective_verbose else "INFO",
       verbose=effective_verbose,
       quiet=(ctx.obj["output_mode"] in ["quiet", "json"])
   )

   ctx.obj["verbose"] = effective_verbose
   ```

4. **上下文传递**: 将 output_mode 存储到 ctx.obj,供子命令使用

5. **帮助文本更新**: 更新 --verbose 帮助文本说明优先级:
   ```python
   @click.option("--verbose", "-v", is_flag=True, help="详细模式:显示调试信息(与 --quiet/--json-output 冲突时被忽略)")
   ```

注意事项:
- 不要修改子命令的实现(子命令通过 ctx.obj["output_mode"] 获取模式)
- 确保 --json-output 和 --quiet 可以独立工作
- 参数顺序不影响优先级(逻辑中显式处理)
  </action>
  <verify>
<verify>
  <automated>pytest tests/cli/test_json_output.py::test_quiet_mode -xvs</automated>
  <manual>手动验证优先级: pixiv-downloader --quiet --verbose version 应为静默模式</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
  </verify>
  <done>
运行 `pixiv-downloader --quiet version` 仅输出 JSON 结果,无进度和日志输出;运行 `pixiv-downloader --json-output --verbose version` 为 JSON 模式(verbose 被忽略)。
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Wave 0 测试文件</name>
  <files>
    tests/cli/test_json_output.py
    tests/cli/conftest.py
  </files>
  <action>
创建静默和 JSON 输出模式的测试文件,以及共享 fixture。

**tests/cli/conftest.py**: 添加共享 fixture
```python
import pytest
from click.testing import CliRunner

@pytest.fixture
def runner():
    """提供 Click CliRunner 实例"""
    return CliRunner(mix_stderr=False)
```

**tests/cli/test_json_output.py**: 测试用例覆盖:

1. **test_quiet_mode**: 验证 --quiet 参数
   - 运行: `runner.invoke(cli, ['--quiet', 'version'])`
   - 断言 stderr 为空(无日志输出)
   - 断言 stdout 包含 JSON 输出
   - 断言 exit_code == 0

2. **test_quiet_disables_verbose**: 验证 --quiet 优先级高于 --verbose
   - 运行: `runner.invoke(cli, ['--quiet', '--verbose', 'version'])`
   - 断言 stderr 为空(verbose 被忽略)

3. **test_json_output_format**: 验证 --json-output 参数
   - 运行: `runner.invoke(cli, ['--json-output', 'version'])`
   - 断言 stdout 是有效 JSON: `json.loads(result.output)`
   - 断言 JSON 包含预期字段(如 "version", "success")

4. **test_json_error_handling**: 验证 JSON 模式下的错误处理
   - 运行无效命令: `runner.invoke(cli, ['--json-output', 'download', '--type', 'invalid'])`
   - 断言输出是有效 JSON(即使是错误)
   - 断言 JSON 包含 "success": false 和 "error" 字段

5. **test_priority_json_over_quiet**: 验证 --json-output 优先级
   - 运行: `runner.invoke(cli, ['--json-output', '--quiet', 'version'])`
   - 断言为 JSON 模式(优先级最高)

注意事项:
- 使用 `CliRunner(mix_stderr=False)` 分离 stdout 和 stderr
- 所有 JSON 断言使用 `json.loads()` 验证有效性
- 不要测试真实网络请求(Mock 如果需要)
  </action>
  <verify>
<verify>
  <automated>pytest tests/cli/test_json_output.py -xvs</automated>
  <manual>检查测试覆盖率: pytest --cov=gallery_dl_auo/cli tests/cli/test_json_output.py</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
  </verify>
  <done>
运行 `pytest tests/cli/test_json_output.py -v` 全部通过,测试覆盖静默模式、JSON 输出、参数优先级、错误处理。
  </done>
</task>

</tasks>

<verification>
验证 CLI-02 和 CLI-03 需求实现:

1. **--quiet 功能验证**:
   - 运行 `pixiv-downloader --quiet version 2>&1` 确认无进度和日志输出
   - 确认仅输出最终 JSON 结果
   - 确认 stderr 为空

2. **--json-output 功能验证**:
   - 运行 `pixiv-downloader --json-output version` 确认输出有效 JSON
   - 运行错误命令确认错误信息也是 JSON 格式
   - 确认 JSON 可被 `jq` 或 Python json 模块解析

3. **参数优先级验证**:
   - `--quiet --verbose` → 静默模式(verbose 被忽略)
   - `--json-output --quiet` → JSON 模式(最高优先级)
   - `--json-output --verbose` → JSON 模式(verbose 被忽略)

4. **测试验证**:
   - `pytest tests/cli/test_json_output.py -v` 全部通过
   - 测试覆盖静默模式、JSON 输出、优先级、错误处理

5. **边界情况**:
   - 子命令可以访问 ctx.obj["output_mode"]
   - 日志文件始终记录(无论模式)
   - 中断信号(Ctrl+C)在静默模式下也输出 JSON
</verification>

<success_criteria>
CLI-02 和 CLI-03 需求达成标准:

1. 用户使用 --quiet 参数时看不到任何进度和日志输出 ✓
2. --quiet 仅输出最终 JSON 结果,stderr 为空 ✓
3. 用户使用 --json-output 参数时所有输出都是有效 JSON ✓
4. --json-output 模式下错误信息也能被 JSON 解析器正确解析 ✓
5. 参数优先级正确: --json-output > --quiet > --verbose ✓
6. 单元测试覆盖所有关键功能,测试全部通过 ✓
7. 第三方工具可以可靠使用静默和 JSON 模式 ✓
</success_criteria>

<output>
完成后创建 `.planning/phases/08.1-cli-api-enhancement/08.1-02-SUMMARY.md`
</output>
