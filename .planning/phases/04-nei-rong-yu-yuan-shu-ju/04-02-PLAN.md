---
phase: 04-nei-rong-yu-yuan-shu-ju
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/gallery_dl_auo/api/pixiv_client.py
  - tests/api/test_pixiv_client.py
autonomous: true
requirements: [CONT-02, CONT-03]

must_haves:
  truths:
    - "程序能够通过 API 获取作品的完整元数据"
    - "元数据包含标签列表(名称和翻译)"
    - "元数据包含统计数据(收藏数、浏览量、评论数)"
    - "API 错误能够被优雅处理"
  artifacts:
    - path: "src/gallery_dl_auo/api/pixiv_client.py"
      provides: "get_artwork_metadata() 方法"
      min_lines: 200
    - path: "tests/api/test_pixiv_client.py"
      provides: "元数据获取测试"
      contains: "test_get_artwork_metadata"
  key_links:
    - from: "PixivClient.get_artwork_metadata()"
      to: "pixivpy3.illust_detail()"
      via: "API 调用"
      pattern: "self.api.illust_detail(illust_id)"
    - from: "get_artwork_metadata()"
      to: "ArtworkMetadata 模型"
      via: "返回值"
      pattern: "-> ArtworkMetadata"
---

<objective>
扩展 PixivClient 添加元数据获取方法

Purpose: 通过 pixivpy3 的 illust_detail() API 获取作品的完整元数据,包括标签和统计数据

Output:
- PixivClient.get_artwork_metadata() 方法
- 完整的错误处理
- 测试覆盖
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-RESEARCH.md

# 依赖 Wave 1 的输出
@.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-01-SUMMARY.md

# 参考现有代码
@src/gallery_dl_auo/api/pixiv_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加元数据获取方法到 PixivClient</name>
  <files>
    src/gallery_dl_auo/api/pixiv_client.py
    tests/api/test_pixiv_client.py
  </files>
  <action>
扩展 PixivClient 类,添加 get_artwork_metadata() 方法来获取作品的完整元数据

**实现步骤**:

1. **扩展 src/gallery_dl_auo/api/pixiv_client.py**:
   在 `get_ranking()` 方法后添加 `get_artwork_metadata()` 方法:

   ```python
   from gallery_dl_auo.models.artwork import (
       ArtworkMetadata,
       ArtworkStatistics,
       ArtworkTag,
   )

   def get_artwork_metadata(self, illust_id: int) -> ArtworkMetadata:
       """获取单个作品的完整元数据

       Args:
           illust_id: 作品 ID

       Returns:
           ArtworkMetadata: 包含完整元数据的模型对象

       Raises:
           PixivAPIError: API 调用失败时抛出
       """
       try:
           logger.info(f"Fetching metadata for illust_id={illust_id}")
           result = self.api.illust_detail(illust_id)
           illust = result.illust

           # 提取标签
           tags = [
               ArtworkTag(
                   name=tag.name,
                   translated_name=tag.translated_name
               )
               for tag in illust.tags
           ]

           # 提取统计数据
           statistics = ArtworkStatistics(
               bookmark_count=illust.total_bookmarks,
               view_count=illust.total_view,
               comment_count=illust.total_comments,
           )

           # 构建元数据模型
           metadata = ArtworkMetadata(
               illust_id=illust.id,
               title=illust.title,
               author=illust.user.name,
               author_id=illust.user.id,
               tags=tags,
               statistics=statistics,
           )

           logger.debug(f"Metadata fetched: {metadata.title} ({len(tags)} tags)")
           return metadata

       except Exception as e:
           error_msg = f"Failed to get metadata for {illust_id}: {e}"
           logger.error(error_msg)
           raise PixivAPIError(error_msg) from e
   ```

**关键点**:
- 导入 ArtworkMetadata, ArtworkStatistics, ArtworkTag 从 models.artwork
- 使用 `self.api.illust_detail(illust_id)` 获取作品详情
- 从 `illust.tags` 提取标签列表,每个标签包含 name 和 translated_name
- 从 `illust.total_bookmarks`, `total_view`, `total_comments` 提取统计数据
- 构建 ArtworkMetadata 对象并返回
- 异常处理: 捕获所有异常,转换为 PixivAPIError 抛出
- 添加详细的日志记录 (info, debug, error)

2. **扩展 tests/api/test_pixiv_client.py**:
   添加测试用例:

   - `test_get_artwork_metadata_success()`: 测试成功获取元数据
     - Mock `api.illust_detail()` 返回包含 tags, total_bookmarks, total_view, total_comments 的对象
     - 验证返回的 ArtworkMetadata 包含正确数据
     - 验证标签列表正确提取

   - `test_get_artwork_metadata_with_translated_tags()`: 测试带翻译的标签
     - Mock 返回包含 translated_name 的标签
     - 验证 translated_name 字段正确提取

   - `test_get_artwork_metadata_without_translated_tags()`: 测试不带翻译的标签
     - Mock 返回不包含 translated_name 的标签 (translated_name = None)
     - 验证 translated_name 为 None

   - `test_get_artwork_metadata_api_error()`: 测试 API 错误处理
     - Mock `api.illust_detail()` 抛出异常
     - 验证抛出 PixivAPIError

**Mock 对象示例**:
```python
# 创建 mock illust 对象
mock_illust = MagicMock()
mock_illust.id = 12345
mock_illust.title = "Test Artwork"
mock_illust.user.name = "Test Author"
mock_illust.user.id = 67890
mock_illust.tags = [
    MagicMock(name="风景", translated_name="landscape"),
    MagicMock(name="插画", translated_name="illustration"),
]
mock_illust.total_bookmarks = 1000
mock_illust.total_view = 5000
mock_illust.total_comments = 50

# Mock API 响应
mock_result = MagicMock()
mock_result.illust = mock_illust
client.api.illust_detail = MagicMock(return_value=mock_result)
```
  </action>
  <verify>
    <automated>pytest tests/api/test_pixiv_client.py::test_get_artwork_metadata_success -x -v</automated>
    <manual>验证新增的 4 个测试通过</manual>
    <sampling_rate>运行在此任务提交后,下一个任务开始前</sampling_rate>
  </verify>
  <done>
- PixivClient.get_artwork_metadata() 方法实现完成
- 方法签名: `def get_artwork_metadata(self, illust_id: int) -> ArtworkMetadata`
- 从 illust_detail() API 提取标签和统计数据
- 异常转换为 PixivAPIError
- 4 个测试全部通过
  </done>
</task>

</tasks>

<verification>
**Wave 2 验证**:
- [ ] get_artwork_metadata() 方法正确提取标签和统计数据
- [ ] 标签列表包含 name 和 translated_name 字段
- [ ] 统计数据包含 bookmark_count, view_count, comment_count 字段
- [ ] API 错误转换为 PixivAPIError
- [ ] 测试覆盖率 >= 90% (新增方法)
- [ ] 所有测试通过: `pytest tests/api/test_pixiv_client.py -x`
- [ ] mypy 类型检查通过: `mypy src/gallery_dl_auo/api/pixiv_client.py`
</verification>

<success_criteria>
**Goal-Backward Verification**:

1. ✅ **程序能够通过 API 获取作品的完整元数据** - get_artwork_metadata() 方法调用 illust_detail() API
2. ✅ **元数据包含标签列表** - 从 illust.tags 提取,每个标签包含 name 和 translated_name
3. ✅ **元数据包含统计数据** - 从 illust.total_bookmarks, total_view, total_comments 提取
4. ✅ **API 错误能够被优雅处理** - 异常捕获并转换为 PixivAPIError

**Must-Haves Check**:
- [ ] PixivClient.get_artwork_metadata() 方法存在
- [ ] 返回类型为 ArtworkMetadata
- [ ] 测试覆盖成功和失败场景
- [ ] 错误日志记录完整
</success_criteria>

<output>
After completion, create `.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-02-SUMMARY.md`
</output>
