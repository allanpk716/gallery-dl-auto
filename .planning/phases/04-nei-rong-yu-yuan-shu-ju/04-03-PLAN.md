---
phase: 04-nei-rong-yu-yuan-shu-ju
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/gallery_dl_auo/download/ranking_downloader.py
  - src/gallery_dl_auo/cli/download_cmd.py
  - tests/download/test_ranking_downloader.py
  - tests/cli/test_download_cmd.py
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04]

must_haves:
  truths:
    - "用户能够通过 --path-template 参数指定保存路径"
    - "下载结果包含完整的元数据(标签和统计数据)"
    - "路径模板能够使用元数据字段构建目录结构"
    - "元数据获取失败时使用排行榜基础数据作为 fallback"
  artifacts:
    - path: "src/gallery_dl_auo/download/ranking_downloader.py"
      provides: "集成元数据获取和路径模板"
      min_lines: 180
    - path: "src/gallery_dl_auo/cli/download_cmd.py"
      provides: "--path-template 参数"
      min_lines: 100
    - path: "tests/download/test_ranking_downloader.py"
      provides: "路径模板集成测试"
      contains: "test_download_with_path_template"
  key_links:
    - from: "RankingDownloader.download_ranking()"
      to: "PathTemplate.render()"
      via: "路径构建"
      pattern: "template.render(context)"
    - from: "download_ranking()"
      to: "PixivClient.get_artwork_metadata()"
      via: "元数据获取"
      pattern: "self.client.get_artwork_metadata(illust['id'])"
    - from: "download_cmd.py"
      to: "RankingDownloader"
      via: "传递 path_template 参数"
      pattern: "downloader.download_ranking(..., path_template=template)"
---

<objective>
集成元数据获取和路径模板系统到下载流程

Purpose: 让用户能够使用自定义路径模板保存图片,并在下载结果中获取完整的元数据

Output:
- RankingDownloader 集成元数据获取和路径模板
- download 命令添加 --path-template 参数
- 下载结果包含元数据字段
- 完整的测试覆盖
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-RESEARCH.md

# 依赖 Wave 1-2 的输出
@.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-01-SUMMARY.md
@.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-02-SUMMARY.md

# 参考现有代码
@src/gallery_dl_auo/download/ranking_downloader.py
@src/gallery_dl_auo/cli/download_cmd.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 RankingDownloader 集成元数据和路径模板</name>
  <files>
    src/gallery_dl_auo/download/ranking_downloader.py
    tests/download/test_ranking_downloader.py
  </files>
  <action>
扩展 RankingDownloader 类,在下载流程中集成元数据获取和路径模板系统

**实现步骤**:

1. **修改 src/gallery_dl_auo/download/ranking_downloader.py**:

   a. **添加导入**:
   ```python
   from gallery_dl_auo.models.artwork import ArtworkMetadata
   from gallery_dl_auo.utils.path_template import PathTemplate
   ```

   b. **修改 download_ranking() 方法签名**:
   ```python
   def download_ranking(
       self,
       mode: str = "day",
       date: str | None = None,
       base_delay: float = 2.5,
       jitter: float = 1.0,
       path_template: str | None = None,  # 新增参数
   ) -> dict:
   ```

   c. **修改 download_ranking() 方法实现**:

   **步骤 1**: 初始化路径模板
   ```python
   # 初始化路径模板 (如果提供)
   template = PathTemplate(path_template) if path_template else None
   ```

   **步骤 2**: 修改目录创建逻辑
   ```python
   # 创建输出目录: 如果未使用路径模板,创建 {mode}-{date}/ 子目录
   if not template:
       ranking_dir = self.output_dir / f"{mode}-{date}"
       ranking_dir.mkdir(parents=True, exist_ok=True)
       logger.info(f"Output directory: {ranking_dir}")
   else:
       # 使用路径模板时,不需要预先创建目录
       # PathTemplate.render() 会生成完整路径
       logger.info(f"Using path template: {path_template}")
   ```

   **步骤 3**: 修改下载循环 (获取元数据 + 构建路径)
   ```python
   for illust in ranking_data:
       # 1. 获取完整元数据
       metadata = None
       try:
           metadata = self.client.get_artwork_metadata(illust['id'])
           logger.debug(f"Metadata fetched: {metadata.title}")
       except PixivAPIError as e:
           logger.warning(f"Failed to get metadata for {illust['id']}: {e}, using basic data")

       # 2. 构建文件路径
       if template:
           # 使用路径模板
           context = {
               "mode": mode,
               "date": date,
               "illust_id": illust['id'],
               "title": metadata.title if metadata else illust['title'],
               "author": metadata.author if metadata else illust['author'],
               "author_id": metadata.author_id if metadata else "unknown",
           }
           filepath = self.output_dir / template.render(context)
           # 确保父目录存在
           filepath.parent.mkdir(parents=True, exist_ok=True)
       else:
           # 默认路径: {output_dir}/{mode}-{date}/{illust_id}_{title}.jpg
           filename = sanitize_filename(f"{illust['id']}_{illust['title']}.jpg")
           filepath = ranking_dir / filename

       logger.info(f"Downloading: {illust['title']} (ID: {illust['id']})")

       # 3. 下载图片
       result = download_file(illust["image_url"], filepath)

       # 4. 记录结果 (包含元数据)
       if result["success"]:
           success_item = {
               "illust_id": illust['id'],
               "title": metadata.title if metadata else illust['title'],
               "author": metadata.author if metadata else illust['author'],
               "filepath": result["filepath"],
           }
           # 添加元数据字段 (如果有)
           if metadata:
               success_item["tags"] = [tag.model_dump() for tag in metadata.tags]
               success_item["statistics"] = metadata.statistics.model_dump()

           results["success"].append(success_item)
       else:
           results["failed"].append({
               "illust_id": illust['id'],
               "title": metadata.title if metadata else illust['title'],
               "error": result["error"],
           })

       # 5. 速率控制
       rate_limit_delay(base_delay, jitter)
   ```

**关键点**:
- 元数据获取失败时使用排行榜基础数据作为 fallback (不中断下载流程)
- 路径模板可选,未提供时使用默认目录结构
- 使用路径模板时,确保父目录存在 (`filepath.parent.mkdir(parents=True, exist_ok=True)`)
- 成功结果包含 tags 和 statistics 字段 (如果有元数据)
- 使用 `model_dump()` 将 Pydantic 模型转换为 dict

2. **扩展 tests/download/test_ranking_downloader.py**:
   添加测试用例:

   - `test_download_with_path_template()`: 测试使用路径模板
     - Mock client.get_ranking() 返回排行榜数据
     - Mock client.get_artwork_metadata() 返回元数据
     - Mock download_file() 返回成功
     - 调用 download_ranking(path_template="{author}/{title}.jpg")
     - 验证文件路径按模板构建

   - `test_download_with_metadata()`: 测试元数据获取
     - Mock client.get_artwork_metadata() 返回完整元数据
     - 验证结果包含 tags 和 statistics 字段

   - `test_download_metadata_fallback()`: 测试元数据获取失败的 fallback
     - Mock client.get_artwork_metadata() 抛出 PixivAPIError
     - 验证使用排行榜基础数据继续下载
     - 验证结果不包含 tags 和 statistics 字段

   - `test_download_with_all_template_variables()`: 测试所有模板变量
     - 模板包含所有 6 个变量: {mode}, {date}, {illust_id}, {title}, {author}, {author_id}
     - 验证所有变量正确替换
  </action>
  <verify>
    <automated>pytest tests/download/test_ranking_downloader.py -x -v</automated>
    <manual>验证新增的 4 个测试通过</manual>
    <sampling_rate>运行在此任务提交后,下一个任务开始前</sampling_rate>
  </verify>
  <done>
- download_ranking() 方法添加 path_template 参数
- 元数据获取集成到下载流程
- 元数据获取失败时使用 fallback
- 路径模板支持所有 6 个变量
- 下载结果包含 tags 和 statistics 字段
- 4 个测试全部通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 download CLI 命令添加路径模板参数</name>
  <files>
    src/gallery_dl_auo/cli/download_cmd.py
    tests/cli/test_download_cmd.py
  </files>
  <action>
扩展 download 命令,添加 --path-template 参数,并更新 JSON 输出格式

**实现步骤**:

1. **修改 src/gallery_dl_auo/cli/download_cmd.py**:

   a. **添加 --path-template 选项**:
   ```python
   @click.option(
       "--path-template",
       default=None,
       help="Path template for saving files (e.g., {author}/{title}.jpg). Variables: {mode}, {date}, {illust_id}, {title}, {author}, {author_id}",
   )
   def download(date: str | None, output: str, mode: str, path_template: str | None) -> None:
   ```

   b. **传递 path_template 参数到 downloader**:
   ```python
   # 3. Execute download
   output_dir = Path(output)
   downloader = RankingDownloader(client, output_dir)

   results = downloader.download_ranking(
       mode=mode,
       date=date,
       path_template=path_template  # 新增参数
   )
   ```

   c. **更新 JSON 输出格式**:
   ```python
   # 4. Output JSON result
   output_data = {
       "success": True,
       "total": results["total"],
       "success_count": len(results["success"]),
       "failed_count": len(results["failed"]),
       "output_dir": str(output_dir),
       "date": date or datetime.date.today().strftime("%Y-%m-%d"),
       "mode": mode,
       "path_template": path_template,  # 新增字段
       "success_list": results["success"],
       "failed_list": results["failed"],
   }
   ```

2. **扩展 tests/cli/test_download_cmd.py**:
   添加测试用例:

   - `test_download_with_path_template()`: 测试使用路径模板参数
     - 调用命令: `download --path-template "{author}/{title}.jpg"`
     - Mock RankingDownloader
     - 验证 path_template 参数正确传递

   - `test_download_json_output_with_metadata()`: 测试 JSON 输出包含元数据
     - Mock downloader 返回包含 tags 和 statistics 的结果
     - 验证 JSON 输出包含元数据字段

   - `test_download_without_path_template()`: 测试不使用路径模板
     - 调用命令不传递 --path-template
     - 验证使用默认目录结构

**关键点**:
- --path-template 参数可选,默认为 None
- help 文本列出所有支持的变量
- JSON 输出包含 path_template 字段
- 保持与现有命令的兼容性
  </action>
  <verify>
    <automated>pytest tests/cli/test_download_cmd.py -x -v</automated>
    <manual>验证新增的 3 个测试通过</manual>
    <sampling_rate>运行在此任务提交后,下一个任务开始前</sampling_rate>
  </verify>
  <done>
- download 命令添加 --path-template 参数
- 参数可选,默认 None
- help 文本列出支持的变量
- JSON 输出包含 path_template 字段
- 3 个测试全部通过
  </done>
</task>

</tasks>

<verification>
**Wave 3 验证**:
- [ ] download_ranking() 方法支持 path_template 参数
- [ ] 元数据获取失败时使用 fallback
- [ ] 路径模板支持所有 6 个变量
- [ ] 下载结果包含 tags 和 statistics 字段
- [ ] download 命令支持 --path-template 参数
- [ ] JSON 输出包含元数据字段
- [ ] 所有测试通过: `pytest tests/download/ tests/cli/ -x`
- [ ] mypy 类型检查通过
</verification>

<success_criteria>
**Goal-Backward Verification**:

1. ✅ **用户能够通过 --path-template 参数指定保存路径** - download 命令添加参数,传递给 downloader
2. ✅ **下载结果包含完整的元数据** - success_list 包含 tags 和 statistics 字段
3. ✅ **路径模板能够使用元数据字段构建目录结构** - 支持 6 个变量,包括 author, title 等
4. ✅ **元数据获取失败时使用排行榜基础数据作为 fallback** - try-except 捕获 PixivAPIError,使用 illust 数据

**Must-Haves Check**:
- [ ] download 命令支持 --path-template 参数
- [ ] 路径模板变量替换正确
- [ ] 元数据包含在下载结果中
- [ ] Fallback 逻辑正确
- [ ] 测试覆盖所有场景
</success_criteria>

<output>
After completion, create `.planning/phases/04-nei-rong-yu-yuan-shu-ju/04-03-SUMMARY.md`
</output>
