---
wave: 2
depends_on: [06-01]
files_modified:
  - src/gallery_dl_auo/download/progress_manager.py
  - src/gallery_dl_auo/download/retry_handler.py
  - tests/download/test_progress_manager.py
  - tests/download/test_retry_handler.py
  - src/gallery_dl_auo/download/ranking_downloader.py
  - tests/download/test_ranking_downloader.py
autonomous: true
---

# Phase 6 - Plan 02: 大规模数据集处理和断点续传

**Wave:** 2 (依赖 Plan 01 完成)
**Dependencies:** Plan 01 (排行榜类型扩展)
**Estimated Duration:** 25-35 minutes

## Objective

实现大规模排行榜数据集(月榜 1500+ 张)的完整下载能力,包括:
1. 自动跟随 pixiv API 的 `next_url` 分页机制
2. 断点续传功能,记录已下载作品 ID
3. 单张图片下载失败时的重试机制
4. 进度文件管理,下载完成后自动清理

## Context

<files_to_read>
- .planning/REQUIREMENTS.md — 需求 RANK-03 (月榜完整下载)
- .planning/phases/06-multi-ranking-support/06-CONTEXT.md — 用户决策
- .planning/phases/06-multi-ranking-support/06-RESEARCH.md — 技术研究
- src/gallery_dl_auo/download/ranking_downloader.py — 现有下载器
- src/gallery_dl_auo/download/file_downloader.py — 现有文件下载器
</files_to_read>

**User Decisions (from CONTEXT.md):**
- 支持断点续传,记录已下载作品 ID
- 进度记录方式: 在下载目录内创建 `.progress.json` 文件
  - 示例: `./pixiv-downloads/weekly-2026-02-18/.progress.json`
  - 使用路径模板时也遵循此规则
- 单张图片下载失败时重试 3 次,间隔 5 秒
- 检测到未完成下载时自动继续,不询问用户
- 下载完成后自动删除 `.progress.json` 文件
- 进度显示: 静默模式,仅在完成时显示总结

**Research Findings:**
- pixivpy3 的 `next_url` + `parse_qs()` 机制可自动处理分页
- 每页约 50 张作品,月榜 1500+ 张需要 30+ 页
- JSON 进度文件使用 Pydantic 模型,包含 mode, date, downloaded_ids, failed_ids
- 重试机制封装为独立函数,可配置重试次数和延迟

## Tasks

### Task 1: 创建进度管理器模块

**<files>**
- `src/gallery_dl_auo/download/progress_manager.py` (新建)
- `tests/download/test_progress_manager.py` (新建)

**<action>**
创建进度管理器,负责加载、保存和更新下载进度:

1. **进度数据模型:**
```python
from pydantic import BaseModel
from pathlib import Path
import json
import logging

logger = logging.getLogger("gallery_dl_auo")

class DownloadProgress(BaseModel):
    """下载进度模型"""
    mode: str
    date: str
    downloaded_ids: set[int] = set()
    failed_ids: set[int] = set()

    class Config:
        # Pydantic v2 配置
        json_encoders = {
            set: list  # 序列化时转换为列表
        }

    def save(self, progress_file: Path) -> None:
        """保存进度到文件

        Args:
            progress_file: 进度文件路径
        """
        # 确保父目录存在
        progress_file.parent.mkdir(parents=True, exist_ok=True)

        # 序列化: set → list
        data = {
            "mode": self.mode,
            "date": self.date,
            "downloaded_ids": sorted(list(self.downloaded_ids)),
            "failed_ids": sorted(list(self.failed_ids))
        }

        # 原子写入: 先写临时文件,再重命名
        temp_file = progress_file.with_suffix('.tmp')
        with open(temp_file, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)

        # 重命名临时文件
        temp_file.rename(progress_file)
        logger.debug(f"Progress saved: {len(self.downloaded_ids)} downloaded, {len(self.failed_ids)} failed")

    @classmethod
    def load(cls, progress_file: Path) -> 'DownloadProgress | None':
        """从文件加载进度

        Args:
            progress_file: 进度文件路径

        Returns:
            DownloadProgress 对象,或 None (文件不存在)
        """
        if not progress_file.exists():
            return None

        try:
            with open(progress_file, 'r', encoding='utf-8') as f:
                data = json.load(f)

            # 反序列化: list → set
            data['downloaded_ids'] = set(data.get('downloaded_ids', []))
            data['failed_ids'] = set(data.get('failed_ids', []))

            progress = cls(**data)
            logger.info(
                f"Progress loaded: {len(progress.downloaded_ids)} already downloaded, "
                f"{len(progress.failed_ids)} previously failed"
            )
            return progress

        except (json.JSONDecodeError, KeyError) as e:
            logger.warning(f"Failed to load progress file: {e}, starting fresh")
            return None

    def mark_downloaded(self, illust_id: int) -> None:
        """标记作品为已下载"""
        self.downloaded_ids.add(illust_id)
        # 如果之前失败过,从失败列表移除
        self.failed_ids.discard(illust_id)

    def mark_failed(self, illust_id: int) -> None:
        """标记作品为失败"""
        self.failed_ids.add(illust_id)

    def is_downloaded(self, illust_id: int) -> bool:
        """检查作品是否已下载"""
        return illust_id in self.downloaded_ids

    def get_progress_file_path(self, output_dir: Path, mode: str, date: str) -> Path:
        """获取进度文件路径

        Args:
            output_dir: 输出目录
            mode: 排行榜模式
            date: 日期字符串

        Returns:
            进度文件路径
        """
        # 用户决策: 进度文件在下载目录内
        # 示例: ./pixiv-downloads/weekly-2026-02-18/.progress.json
        return output_dir / f"{mode}-{date}" / ".progress.json"
```

2. **单元测试:**
创建 `tests/download/test_progress_manager.py`:
```python
import pytest
from pathlib import Path
from gallery_dl_auo.download.progress_manager import DownloadProgress

def test_progress_save_and_load(tmp_path):
    """测试进度保存和加载"""
    progress_file = tmp_path / ".progress.json"

    # 创建进度
    progress = DownloadProgress(
        mode="week",
        date="2026-02-18",
        downloaded_ids={1, 2, 3},
        failed_ids={4}
    )

    # 保存
    progress.save(progress_file)
    assert progress_file.exists()

    # 加载
    loaded = DownloadProgress.load(progress_file)
    assert loaded is not None
    assert loaded.mode == "week"
    assert loaded.date == "2026-02-18"
    assert loaded.downloaded_ids == {1, 2, 3}
    assert loaded.failed_ids == {4}

def test_progress_load_nonexistent(tmp_path):
    """测试加载不存在的进度文件"""
    progress_file = tmp_path / "nonexistent.json"
    loaded = DownloadProgress.load(progress_file)
    assert loaded is None

def test_progress_load_corrupted(tmp_path):
    """测试加载损坏的进度文件"""
    progress_file = tmp_path / "corrupted.json"
    progress_file.write_text("invalid json {")

    loaded = DownloadProgress.load(progress_file)
    assert loaded is None  # 优雅降级

def test_mark_operations():
    """测试标记操作"""
    progress = DownloadProgress(mode="day", date="2026-02-25")

    # 标记已下载
    progress.mark_downloaded(1)
    assert progress.is_downloaded(1)
    assert 1 in progress.downloaded_ids

    # 标记失败
    progress.mark_failed(2)
    assert 2 in progress.failed_ids

    # 重新下载成功的作品,从失败列表移除
    progress.mark_failed(3)
    progress.mark_downloaded(3)
    assert 3 not in progress.failed_ids
    assert 3 in progress.downloaded_ids

def test_progress_file_path():
    """测试进度文件路径生成"""
    output_dir = Path("./pixiv-downloads")
    path = DownloadProgress(
        mode="week",
        date="2026-02-18"
    ).get_progress_file_path(output_dir, "week", "2026-02-18")

    expected = Path("./pixiv-downloads/week-2026-02-18/.progress.json")
    assert path == expected
```

**<verify>**
```xml
<verify>
  <automated>pytest tests/download/test_progress_manager.py -x -v</automated>
  <manual>手动检查: 创建进度文件,验证 JSON 格式可读</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- 进度管理器支持保存和加载进度
- 支持标记已下载和失败的作品
- 优雅处理损坏的进度文件
- 单元测试覆盖所有场景

---

### Task 2: 创建重试处理器模块

**<files>**
- `src/gallery_dl_auo/download/retry_handler.py` (新建)
- `tests/download/test_retry_handler.py` (新建)

**<action>**
创建重试处理器,封装下载失败重试逻辑:

1. **重试处理器函数:**
```python
import logging
import time
from typing import Callable, TypeVar

logger = logging.getLogger("gallery_dl_auo")

T = TypeVar('T')

def retry_on_failure(
    func: Callable[[], T],
    max_retries: int = 3,
    retry_delay: float = 5.0,
) -> T:
    """重试包装器

    Args:
        func: 要执行的函数
        max_retries: 最大重试次数 (默认 3)
        retry_delay: 重试间隔秒数 (默认 5.0)

    Returns:
        函数返回值

    Raises:
        Exception: 重试耗尽后抛出最后一次异常

    Example:
        >>> result = retry_on_failure(
        ...     lambda: download_file(url, path),
        ...     max_retries=3,
        ...     retry_delay=5.0
        ... )
    """
    last_error: Exception | None = None

    for attempt in range(max_retries):
        try:
            result = func()
            return result

        except Exception as e:
            last_error = e
            logger.warning(
                f"Operation failed (attempt {attempt + 1}/{max_retries}): {e}"
            )

            if attempt < max_retries - 1:
                logger.info(f"Retrying in {retry_delay} seconds...")
                time.sleep(retry_delay)

    # 重试耗尽,抛出最后一次异常
    error_msg = f"Failed after {max_retries} attempts"
    logger.error(error_msg)
    raise Exception(error_msg) from last_error

def retry_download_file(
    download_func: Callable[[], dict],
    max_retries: int = 3,
    retry_delay: float = 5.0,
) -> dict:
    """重试文件下载

    专门用于文件下载的重试包装器,处理返回 dict 的下载函数。

    Args:
        download_func: 下载函数,返回 {'success': bool, 'error': str, ...}
        max_retries: 最大重试次数
        retry_delay: 重试间隔秒数

    Returns:
        下载结果字典 {'success': bool, 'error': str, ...}

    Note:
        如果所有重试都失败,返回最后一次的错误结果,不抛出异常
    """
    last_result: dict | None = None

    for attempt in range(max_retries):
        result = download_func()

        if result.get('success'):
            return result

        last_result = result
        error = result.get('error', 'Unknown error')
        logger.warning(
            f"Download failed (attempt {attempt + 1}/{max_retries}): {error}"
        )

        if attempt < max_retries - 1:
            logger.info(f"Retrying in {retry_delay} seconds...")
            time.sleep(retry_delay)

    # 重试耗尽,返回最后一次错误
    logger.error(f"Download failed after {max_retries} attempts")
    if last_result:
        last_result['retries_exhausted'] = True
    return last_result or {'success': False, 'error': 'No result'}
```

2. **单元测试:**
创建 `tests/download/test_retry_handler.py`:
```python
import pytest
from gallery_dl_auo.download.retry_handler import retry_on_failure, retry_download_file

def test_retry_success_first_attempt():
    """测试第一次成功"""
    call_count = 0

    def operation():
        nonlocal call_count
        call_count += 1
        return "success"

    result = retry_on_failure(operation, max_retries=3, retry_delay=0.1)
    assert result == "success"
    assert call_count == 1

def test_retry_success_after_failures():
    """测试失败后重试成功"""
    call_count = 0

    def operation():
        nonlocal call_count
        call_count += 1
        if call_count < 3:
            raise ValueError("Temporary error")
        return "success"

    result = retry_on_failure(operation, max_retries=3, retry_delay=0.1)
    assert result == "success"
    assert call_count == 3

def test_retry_all_attempts_fail():
    """测试所有重试都失败"""
    call_count = 0

    def operation():
        nonlocal call_count
        call_count += 1
        raise ValueError("Persistent error")

    with pytest.raises(Exception) as exc_info:
        retry_on_failure(operation, max_retries=2, retry_delay=0.1)

    assert "Failed after 2 attempts" in str(exc_info.value)
    assert call_count == 2

def test_retry_download_dict_success():
    """测试下载字典返回成功"""
    call_count = 0

    def download():
        nonlocal call_count
        call_count += 1
        if call_count == 1:
            return {'success': False, 'error': 'Network error'}
        return {'success': True, 'filepath': '/path/to/file'}

    result = retry_download_file(download, max_retries=3, retry_delay=0.1)
    assert result['success'] is True
    assert call_count == 2

def test_retry_download_all_fail():
    """测试下载全部失败"""
    def download():
        return {'success': False, 'error': 'Network error'}

    result = retry_download_file(download, max_retries=2, retry_delay=0.1)
    assert result['success'] is False
    assert result['retries_exhausted'] is True
```

**<verify>**
```xml
<verify>
  <automated>pytest tests/download/test_retry_handler.py -x -v</automated>
  <manual>手动检查: 重试日志输出清晰</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- 重试处理器支持自定义重试次数和延迟
- 下载专用重试函数处理 dict 返回值
- 单元测试覆盖成功、失败和重试耗尽场景

---

### Task 3: 扩展 PixivClient 支持完整分页

**<files>**
- `src/gallery_dl_auo/api/pixiv_client.py` (修改)
- `tests/api/test_pixiv_client.py` (修改)

**<action>**
扩展 `PixivClient.get_ranking()` 方法,添加自动跟随 `next_url` 的能力:

1. **添加 get_ranking_all() 方法:**
```python
def get_ranking_all(
    self, mode: str = "day", date: str | None = None
) -> list[dict[str, Any]]:
    """获取排行榜所有数据(自动跟随 next_url)

    Args:
        mode: 排行榜模式
        date: 日期字符串 YYYY-MM-DD (默认为今天)

    Returns:
        作品列表,包含所有页面的数据

    Raises:
        PixivAPIError: API 调用失败时抛出
    """
    try:
        # 构建参数
        params: dict[str, Any] = {"mode": mode}
        if date:
            params["date"] = date

        # 获取第一页数据
        logger.info(f"Fetching ranking: mode={mode}, date={date}")
        json_result = self.api.illust_ranking(**params)

        if not json_result or not hasattr(json_result, "illusts"):
            raise PixivAPIError("Empty ranking result")

        # 累积作品
        all_works = []
        all_works.extend(self._extract_works(json_result.illusts))
        page_count = 1

        # 自动跟随 next_url
        next_url = getattr(json_result, "next_url", None)
        while next_url:
            page_count += 1
            logger.debug(f"Fetching page {page_count}: {next_url}")
            next_qs = self.api.parse_qs(next_url)
            json_result = self.api.illust_ranking(**next_qs)

            if not json_result or not hasattr(json_result, "illusts"):
                break

            all_works.extend(self._extract_works(json_result.illusts))
            next_url = getattr(json_result, "next_url", None)

        logger.info(
            f"Fetched {len(all_works)} works from {page_count} pages"
        )
        return all_works

    except PixivAPIError:
        raise
    except Exception as e:
        error_msg = f"Failed to get ranking: {e}"
        logger.error(error_msg)
        raise PixivAPIError(error_msg) from e

def _extract_works(self, illusts: list) -> list[dict]:
    """从 illusts 列表提取作品数据

    Args:
        illusts: API 返回的 illusts 列表

    Returns:
        作品字典列表
    """
    works = []
    for illust in illusts:
        work = {
            "id": illust.id,
            "title": illust.title,
            "author": illust.user.name,
            "image_url": illust.image_urls.large,
        }
        works.append(work)
    return works
```

2. **添加测试用例:**
```python
def test_get_ranking_all_multiple_pages(mock_api, pixiv_client):
    """测试多页排行榜获取"""
    # 模拟第一页
    page1 = Mock()
    page1.illusts = [create_mock_illust(i) for i in range(1, 51)]
    page1.next_url = "https://api.pixiv.net/ranking?offset=50"

    # 模拟第二页
    page2 = Mock()
    page2.illusts = [create_mock_illust(i) for i in range(51, 101)]
    page2.next_url = None

    mock_api.illust_ranking.side_effect = [page1, page2]
    mock_api.parse_qs.return_value = {"mode": "day", "offset": 50}

    result = pixiv_client.get_ranking_all(mode="day")

    assert len(result) == 100  # 两页共 100 个作品
    assert mock_api.illust_ranking.call_count == 2

def test_get_ranking_all_single_page(mock_api, pixiv_client):
    """测试单页排行榜"""
    mock_result = Mock()
    mock_result.illusts = [create_mock_illust(1), create_mock_illust(2)]
    mock_result.next_url = None
    mock_api.illust_ranking.return_value = mock_result

    result = pixiv_client.get_ranking_all(mode="day")

    assert len(result) == 2
    mock_api.illust_ranking.assert_called_once()
```

**<verify>**
```xml
<verify>
  <automated>pytest tests/api/test_pixiv_client.py::test_get_ranking_all_multiple_pages -x -v</automated>
  <manual>手动测试: 使用真实 API 调用月榜,验证获取 1500+ 张作品</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- `get_ranking_all()` 方法自动跟随 `next_url`
- 支持获取任意大小的排行榜数据
- 测试覆盖单页和多页场景

---

### Task 4: 集成断点续传到 RankingDownloader

**<files>**
- `src/gallery_dl_auo/download/ranking_downloader.py` (修改)
- `tests/download/test_ranking_downloader.py` (修改)

**<action>**
修改 `RankingDownloader.download_ranking()` 方法,集成断点续传和重试机制:

1. **更新 download_ranking() 方法:**
```python
from gallery_dl_auo.download.progress_manager import DownloadProgress
from gallery_dl_auo.download.retry_handler import retry_download_file

def download_ranking(
    self,
    mode: str = "day",
    date: str | None = None,
    base_delay: float = 2.5,
    jitter: float = 1.0,
    path_template: str | None = None,
    enable_resume: bool = True,
    max_retries: int = 3,
    retry_delay: float = 5.0,
) -> dict:
    """下载指定排行榜的所有图片(支持断点续传)

    Args:
        mode: 排行榜模式 (day, week, month 等)
        date: 日期字符串 YYYY-MM-DD (默认今天)
        base_delay: 基础延迟秒数
        jitter: 随机抖动范围
        path_template: 路径模板字符串 (可选)
        enable_resume: 是否启用断点续传 (默认 True)
        max_retries: 单张图片最大重试次数 (默认 3)
        retry_delay: 重试间隔秒数 (默认 5.0)

    Returns:
        dict: 下载结果统计
    """
    # 1. 获取排行榜数据(使用 get_ranking_all)
    if date is None:
        date = datetime.date.today().strftime("%Y-%m-%d")

    try:
        logger.info(f"Fetching ranking: mode={mode}, date={date}")
        ranking_data = self.client.get_ranking_all(mode=mode, date=date)
    except PixivAPIError as e:
        logger.error(f"API error: {e}")
        return {
            "total": 0,
            "success": [],
            "failed": [],
            "error": f"API 错误: {e}",
        }

    # 2. 加载或初始化进度
    progress_file = DownloadProgress(
        mode=mode, date=date
    ).get_progress_file_path(self.output_dir, mode, date)

    if enable_resume and progress_file.exists():
        progress = DownloadProgress.load(progress_file)
        if progress:
            logger.info(
                f"Resuming download: {len(progress.downloaded_ids)} already completed"
            )
    else:
        progress = DownloadProgress(mode=mode, date=date)

    # 3. 初始化路径模板
    template = PathTemplate(path_template) if path_template else None

    # 4. 创建输出目录
    if not template:
        ranking_dir = self.output_dir / f"{mode}-{date}"
        ranking_dir.mkdir(parents=True, exist_ok=True)
        logger.info(f"Output directory: {ranking_dir}")
    else:
        logger.info(f"Using path template: {path_template}")

    # 5. 遍历排行榜,跳过已下载
    results: dict = {
        "total": len(ranking_data),
        "success": [],
        "failed": [],
    }

    for illust in ranking_data:
        illust_id = illust['id']

        # 跳过已下载
        if progress.is_downloaded(illust_id):
            logger.debug(f"Skipping already downloaded: {illust_id}")
            continue

        # 获取元数据
        metadata = self._fetch_metadata_safe(illust_id)

        # 构建文件路径
        filepath = self._build_filepath(
            illust, metadata, template, mode, date, ranking_dir if not template else None
        )

        logger.info(f"Downloading: {illust['title']} (ID: {illust_id})")

        # 下载图片(带重试)
        result = retry_download_file(
            lambda: download_file(illust["image_url"], filepath),
            max_retries=max_retries,
            retry_delay=retry_delay
        )

        # 记录结果
        if result["success"]:
            progress.mark_downloaded(illust_id)
            results["success"].append(self._build_success_item(illust, metadata, result))
            logger.debug(f"Successfully downloaded: {filepath}")
        else:
            progress.mark_failed(illust_id)
            results["failed"].append({
                "illust_id": illust_id,
                "title": metadata.title if metadata else illust['title'],
                "error": result.get("error", "Unknown error"),
            })
            logger.error(f"Failed to download {illust['title']}: {result.get('error')}")

        # 保存进度
        if enable_resume:
            progress.save(progress_file)

        # 速率控制
        rate_limit_delay(base_delay, jitter)

    # 6. 下载完成,删除进度文件
    if enable_resume and progress_file.exists():
        progress_file.unlink()
        logger.info("Download complete, progress file removed")

    logger.info(
        f"Download complete: {len(results['success'])} success, {len(results['failed'])} failed"
    )

    return results
```

2. **添加测试用例:**
```python
def test_download_ranking_with_resume(tmp_path, mock_client):
    """测试断点续传"""
    # 创建部分进度文件
    progress_file = tmp_path / "week-2026-02-18" / ".progress.json"
    progress_file.parent.mkdir(parents=True, exist_ok=True)

    progress = DownloadProgress(
        mode="week",
        date="2026-02-18",
        downloaded_ids={1},  # 已下载第一个作品
        failed_ids=set()
    )
    progress.save(progress_file)

    # 模拟排行榜数据
    mock_client.get_ranking_all.return_value = [
        {'id': 1, 'title': 'Work 1', 'author': 'Author 1', 'image_url': 'url1'},
        {'id': 2, 'title': 'Work 2', 'author': 'Author 2', 'image_url': 'url2'},
    ]

    downloader = RankingDownloader(mock_client, tmp_path)
    result = downloader.download_ranking(mode="week", date="2026-02-18")

    # 验证跳过了已下载的作品
    assert result['total'] == 2
    assert len(result['success']) == 1  # 只下载了第二个

    # 验证进度文件被删除
    assert not progress_file.exists()

def test_download_ranking_retry_on_failure(mock_client, tmp_path):
    """测试下载失败重试"""
    mock_client.get_ranking_all.return_value = [
        {'id': 1, 'title': 'Work 1', 'author': 'Author 1', 'image_url': 'url1'},
    ]

    # 模拟第一次失败,第二次成功
    call_count = 0
    original_download = download_file

    def mock_download(url, path):
        nonlocal call_count
        call_count += 1
        if call_count == 1:
            return {'success': False, 'error': 'Network error'}
        return original_download(url, path)

    downloader = RankingDownloader(mock_client, tmp_path)
    # 使用 monkeypatch 替换 download_file
    # ...
```

**<verify>**
```xml
<verify>
  <automated>pytest tests/download/test_ranking_downloader.py -x -v</automated>
  <manual>手动测试: 中断下载后重新运行,验证从断点继续</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- 断点续传功能集成到下载器
- 单张图片下载失败自动重试 3 次
- 进度文件在下载目录内,下载完成后自动删除
- 测试覆盖断点续传和重试场景

---

## Phase Requirements Coverage

| Req ID | Behavior | Covered by Task |
|--------|----------|-----------------|
| RANK-03 | 月榜(1500+ 张)能完整下载,不因 API offset 限制中断 | Task 3 (自动跟随 next_url) |

## Success Criteria

- [x] 月榜能获取全部 1500+ 张作品,无数据丢失
- [x] 断点续传支持从上次中断处继续
- [x] 单张图片下载失败自动重试 3 次,间隔 5 秒
- [x] 进度文件在下载目录内,下载完成后自动删除
- [x] 所有单元测试和集成测试通过

## Next Plan

**Plan 03:** 配置文件管理和 CLI 集成 — 添加 Hydra 配置支持自定义下载参数(batch_size, batch_delay, concurrency, image_delay),更新 CLI 命令使用配置
