---
wave: 1
depends_on: []
files_modified:
  - src/gallery_dl_auo/cli/validators.py
  - tests/cli/test_validators.py
  - src/gallery_dl_auo/api/pixiv_client.py
  - tests/api/test_pixiv_client.py
autonomous: true
---

# Phase 6 - Plan 01: 排行榜类型扩展和验证

**Wave:** 1 (独立执行)
**Dependencies:** None
**Estimated Duration:** 15-25 minutes

## Objective

扩展 CLI 和 API 以支持全部 13 种排行榜类型,包括类型验证、日期验证和 mode 参数映射。确保用户能够通过 `--type` 参数指定任何排行榜类型,并提供清晰的错误提示。

## Context

<files_to_read>
- .planning/REQUIREMENTS.md — 需求 RANK-02, RANK-03
- .planning/phases/06-multi-ranking-support/06-CONTEXT.md — 用户决策
- .planning/phases/06-multi-ranking-support/06-RESEARCH.md — 技术研究
- src/gallery_dl_auo/cli/download_cmd.py — 现有 download 命令
- src/gallery_dl_auo/api/pixiv_client.py — 现有 PixivClient
</files_to_read>

**User Decisions (from CONTEXT.md):**
- 使用单命令 + `--type` 参数方式
- 支持全部 13 种类型: daily, weekly, monthly, day_male, day_female, week_original, week_rookie, day_manga, day_r18, day_male_r18, day_female_r18, week_r18, week_r18g
- `--type` 参数值使用完整单词,无默认值,必须显式指定
- 无效类型时立即报错: `Invalid ranking type 'xyz'. Valid types: daily, weekly, monthly, ...`
- 支持历史排行榜下载,使用 `--date` 参数,格式 `YYYY-MM-DD`
- 拒绝未来日期,报错: `Date '2026-03-01' is in the future`

**Research Findings:**
- pixivpy3 API 使用 mode 参数:`day`, `week`, `month`, `day_male`, `day_female`, `week_original`, `week_rookie`, `day_manga`, `day_r18`, `day_male_r18`, `day_female_r18`, `week_r18`, `week_r18g`
- 用户友好的名称需要映射到 API mode 参数: `weekly` → `week`, `monthly` → `month`

## Tasks

### Task 1: 创建类型和日期验证器模块

**<files>**
- `src/gallery_dl_auo/cli/validators.py` (新建)
- `tests/cli/test_validators.py` (新建)

**<action>**
创建新的验证器模块,包含:

1. **排行榜类型映射和验证:**
```python
from typing import Literal

# API mode 参数
RANKING_MODES = {
    # 常规
    "daily": "day",
    "weekly": "week",
    "monthly": "month",
    # 分类
    "day_male": "day_male",
    "day_female": "day_female",
    "week_original": "week_original",
    "week_rookie": "week_rookie",
    "day_manga": "day_manga",
    # R18
    "day_r18": "day_r18",
    "day_male_r18": "day_male_r18",
    "day_female_r18": "day_female_r18",
    "week_r18": "week_r18",
    "week_r18g": "week_r18g",
}

RankingType = Literal[
    "daily", "weekly", "monthly",
    "day_male", "day_female", "week_original", "week_rookie", "day_manga",
    "day_r18", "day_male_r18", "day_female_r18", "week_r18", "week_r18g"
]

def validate_ranking_type(type_str: str) -> str:
    """验证排行榜类型并返回 API mode 参数

    Args:
        type_str: 用户输入的排行榜类型

    Returns:
        API mode 参数 (day, week, month 等)

    Raises:
        ValueError: 无效的排行榜类型
    """
    if type_str not in RANKING_MODES:
        valid_types = ", ".join(sorted(RANKING_MODES.keys()))
        raise ValueError(
            f"Invalid ranking type '{type_str}'. Valid types: {valid_types}"
        )
    return RANKING_MODES[type_str]
```

2. **日期验证:**
```python
from datetime import datetime, date

def validate_ranking_date(date_str: str) -> str:
    """验证日期格式和有效性

    Args:
        date_str: 用户输入的日期字符串

    Returns:
        验证后的日期字符串 (YYYY-MM-DD)

    Raises:
        ValueError: 日期格式无效或为未来日期
    """
    # 验证格式
    try:
        parsed = datetime.strptime(date_str, "%Y-%m-%d").date()
    except ValueError:
        raise ValueError(
            f"Invalid date '{date_str}'. Format: YYYY-MM-DD"
        )

    # 验证不是未来日期
    today = date.today()
    if parsed > today:
        raise ValueError(
            f"Date '{date_str}' is in the future"
        )

    return date_str
```

3. **Click 参数验证器:**
```python
import click

def validate_type_param(ctx, param, value: str | None) -> str | None:
    """Click 参数验证器:排行榜类型"""
    if value is None:
        return None
    try:
        return validate_ranking_type(value)
    except ValueError as e:
        raise click.BadParameter(str(e))

def validate_date_param(ctx, param, value: str | None) -> str | None:
    """Click 参数验证器:日期"""
    if value is None:
        return None
    try:
        return validate_ranking_date(value)
    except ValueError as e:
        raise click.BadParameter(str(e))
```

4. **单元测试:**
创建 `tests/cli/test_validators.py`,测试所有验证函数:
- 有效类型转换为正确的 API mode
- 无效类型抛出 ValueError 并包含有效类型列表
- 有效日期格式通过验证
- 无效日期格式抛出 ValueError
- 未来日期抛出 ValueError
- Click 验证器集成测试

**<verify>**
```xml
<verify>
  <automated>pytest tests/cli/test_validators.py -x -v</automated>
  <manual>手动测试: python -m gallery_dl_auo.cli.main download --type invalid_type,应显示友好错误信息</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- 验证器模块包含类型和日期验证函数
- 所有 13 种排行榜类型映射正确
- 单元测试覆盖所有验证场景,测试通过
- 错误信息符合用户决策格式

---

### Task 2: 扩展 PixivClient.get_ranking() 支持 mode 参数

**<files>**
- `src/gallery_dl_auo/api/pixiv_client.py` (修改)
- `tests/api/test_pixiv_client.py` (修改)

**<action>**
修改 `PixivClient.get_ranking()` 方法,确保 `mode` 参数支持所有排行榜类型:

1. **更新方法签名和文档:**
```python
def get_ranking(
    self, mode: str = "day", date: str | None = None
) -> list[dict[str, Any]]:
    """获取排行榜数据

    获取指定模式和日期的排行榜数据,支持分页。

    Args:
        mode: 排行榜模式
            - 常规: day, week, month
            - 分类: day_male, day_female, week_original, week_rookie, day_manga
            - R18: day_r18, day_male_r18, day_female_r18, week_r18, week_r18g
        date: 日期字符串 YYYY-MM-DD (默认为今天)

    Returns:
        作品列表,每个作品包含:
            - id: 作品 ID
            - title: 标题
            - author: 作者名
            - image_url: 图片 URL

    Raises:
        PixivAPIError: API 调用失败时抛出
    """
    # 现有实现已经支持 mode 参数,无需修改逻辑
    # 仅需更新文档字符串
```

**注意:** 现有 `get_ranking()` 实现已经支持 `mode` 参数,此任务主要是:
- 更新文档字符串,明确列出所有支持的 mode 值
- 确保测试覆盖不同的 mode 参数

2. **添加测试用例:**
在 `tests/api/test_pixiv_client.py` 中添加测试:
```python
@pytest.mark.parametrize("mode", [
    "day", "week", "month",
    "day_male", "day_female", "week_original", "week_rookie", "day_manga",
    "day_r18", "day_male_r18", "day_female_r18", "week_r18", "week_r18g"
])
def test_get_ranking_different_modes(mock_api, pixiv_client, mode):
    """测试不同排行榜模式"""
    mock_result = Mock()
    mock_result.illusts = [create_mock_illust(1)]
    mock_result.next_url = None
    mock_api.illust_ranking.return_value = mock_result

    result = pixiv_client.get_ranking(mode=mode)

    assert len(result) == 1
    mock_api.illust_ranking.assert_called_once_with(mode=mode)

def test_get_ranking_with_date(mock_api, pixiv_client):
    """测试带日期的排行榜"""
    mock_result = Mock()
    mock_result.illusts = [create_mock_illust(1)]
    mock_result.next_url = None
    mock_api.illust_ranking.return_value = mock_result

    result = pixiv_client.get_ranking(mode="week", date="2026-02-18")

    assert len(result) == 1
    mock_api.illust_ranking.assert_called_once_with(mode="week", date="2026-02-18")
```

**<verify>**
```xml
<verify>
  <automated>pytest tests/api/test_pixiv_client.py::test_get_ranking_different_modes -x -v</automated>
  <manual>手动测试: 使用真实 API 调用不同 mode,验证返回数据</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- `get_ranking()` 方法文档更新,包含所有支持的 mode 值
- 测试覆盖所有 13 种排行榜类型
- 带日期参数的排行榜测试通过

---

### Task 3: 集成验证器到 download 命令

**<files>**
- `src/gallery_dl_auo/cli/download_cmd.py` (修改)
- `tests/cli/test_download_cmd.py` (修改)

**<action>**
修改 `download` 命令,添加 `--type` 参数并集成验证器:

1. **添加 --type 参数:**
```python
import click
from gallery_dl_auo.cli.validators import validate_type_param, validate_date_param

@download.command()
@click.option(
    '--type',
    callback=validate_type_param,
    required=True,
    help='排行榜类型 (daily, weekly, monthly, day_male, day_female, week_original, week_rookie, day_manga, day_r18, day_male_r18, day_female_r18, week_r18, week_r18g)'
)
@click.option(
    '--date',
    callback=validate_date_param,
    default=None,
    help='历史排行榜日期 (YYYY-MM-DD)'
)
@click.option('--output', '-o', default='./pixiv-downloads', help='输出目录')
@click.pass_obj
def download(config, type: str, date: str | None, output: str):
    """下载指定排行榜的图片"""
    # type 已经通过验证器转换为 API mode 参数
    mode = type  # 类型转换已完成,type 即为 mode

    # 后续逻辑保持不变
    # ...
```

2. **更新测试用例:**
在 `tests/cli/test_download_cmd.py` 中:
```python
def test_download_weekly_ranking(runner, mock_client):
    """测试周榜下载"""
    result = runner.invoke(download, ['--type', 'weekly'])

    assert result.exit_code == 0
    # 验证调用了 week mode
    mock_client.get_ranking.assert_called_once()

def test_download_with_date(runner, mock_client):
    """测试历史排行榜下载"""
    result = runner.invoke(download, ['--type', 'daily', '--date', '2026-02-18'])

    assert result.exit_code == 0

def test_download_invalid_type(runner, mock_client):
    """测试无效排行榜类型"""
    result = runner.invoke(download, ['--type', 'invalid_type'])

    assert result.exit_code != 0
    assert "Invalid ranking type" in result.output
    assert "Valid types:" in result.output

def test_download_future_date(runner, mock_client):
    """测试未来日期"""
    result = runner.invoke(download, ['--type', 'daily', '--date', '2099-12-31'])

    assert result.exit_code != 0
    assert "is in the future" in result.output

def test_download_missing_type(runner, mock_client):
    """测试缺少必需的 --type 参数"""
    result = runner.invoke(download, [])

    assert result.exit_code != 0
    assert "Missing option" in result.output or "--type" in result.output
```

**<verify>**
```xml
<verify>
  <automated>pytest tests/cli/test_download_cmd.py -x -v</automated>
  <manual>手动测试: pixiv-downloader download --type weekly,验证命令执行</manual>
  <sampling_rate>run after this task commits, before next task begins</sampling_rate>
</verify>
```

**<done>**
- `--type` 参数为必需参数,支持所有 13 种类型
- `--date` 参数可选,验证格式和有效性
- 错误信息友好,符合用户决策格式
- 所有测试用例通过

---

## Phase Requirements Coverage

| Req ID | Behavior | Covered by Task |
|--------|----------|-----------------|
| RANK-02 | 用户能够下载 pixiv 每周排行榜 | Task 3 (weekly → week 映射) |
| RANK-03 | 用户能够下载 pixiv 每月排行榜 | Task 3 (monthly → month 映射) |

## Success Criteria

- [x] 用户能够指定任意 13 种排行榜类型
- [x] 无效类型提供清晰的错误提示
- [x] 历史排行榜下载支持 `--date` 参数
- [x] 未来日期被拒绝并报错
- [x] 所有单元测试和集成测试通过

## Next Plan

**Plan 02:** 大规模数据集处理和断点续传 — 扩展 `get_ranking()` 自动跟随 `next_url`,实现进度文件和重试机制,支持月榜 1500+ 张完整下载
