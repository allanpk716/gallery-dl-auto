---
phase: 06-multi-ranking-support
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - tests/download/test_ranking_downloader.py
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md 中 UX-06 需求映射准确反映 Phase 6 实现状态"
    - "test_download_ranking_rate_limiting 测试断言与实际实现匹配"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      issue: "UX-06 标记为 Phase 8 Pending,应标记为 Phase 6 Complete"
      provides: "准确的需求追踪"
    - path: "tests/download/test_ranking_downloader.py"
      issue: "Line 162 断言使用位置参数,实际调用使用关键字参数"
      provides: "正确的测试验证"
  key_links:
    - from: "REQUIREMENTS.md"
      to: "Phase 6 实现"
      via: "Traceability 表格映射"
      pattern: "UX-06.*Phase 6.*Complete"
    - from: "test_ranking_downloader.py"
      to: "rate_limiter.py"
      via: "assert_called_once_with 参数格式"
      pattern: "jitter=1.0"
---

<objective>
修复 Phase 6 验证发现的 2 个非阻塞问题:更新 REQUIREMENTS.md 中的需求映射,修正测试断言参数格式。

Purpose: 确保需求文档与实现状态同步,测试代码与实际实现匹配。
Output: 更新后的 REQUIREMENTS.md 和测试文件,所有测试通过。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-ranking-support/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 REQUIREMENTS.md 中 UX-06 需求映射</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    更新 REQUIREMENTS.md 的 Traceability 部分,修正 UX-06 需求的映射:

    **当前状态 (Line 109):**
    ```markdown
    | UX-06 | Phase 8: 用户体验优化 | Pending |
    ```

    **修改为:**
    ```markdown
    | UX-06 | Phase 6: 多排行榜支持 | Complete |
    ```

    **原因:**
    - UX-06 需求 "用户能够配置请求间隔和并发数" 已在 Phase 6 完成
    - 通过 DownloadConfig 配置模型实现 (src/gallery_dl_auo/config/download_config.py)
    - 配置文件已存在 (config/download.yaml)
    - RankingDownloader 已集成配置参数

    **验证:**
    - 确认 Traceability 表格中 UX-06 行显示 "Phase 6: 多排行榜支持 | Complete"
  </action>
  <verify>
    <automated>grep -n "UX-06.*Phase 6.*Complete" .planning/REQUIREMENTS.md</automated>
    <manual>检查 REQUIREMENTS.md Traceability 部分,UX-06 应显示为 Phase 6 Complete</manual>
  </verify>
  <done>REQUIREMENTS.md 中 UX-06 需求映射从 Phase 8 Pending 更新为 Phase 6 Complete</done>
</task>

<task type="auto">
  <name>Task 2: 修正测试断言参数格式</name>
  <files>tests/download/test_ranking_downloader.py</files>
  <action>
    修复 test_download_ranking_rate_limiting 测试中的断言参数格式:

    **当前代码 (Line 162):**
    ```python
    mock_delay.assert_called_once_with(2.5, 1.0)  # 使用默认配置
    ```

    **修改为:**
    ```python
    mock_delay.assert_called_once_with(2.5, jitter=1.0)  # 使用默认配置
    ```

    **原因:**
    - rate_limit_delay 函数签名: `rate_limit_delay(base_seconds: float = 2.5, jitter: float = 1.0)`
    - 实际调用使用关键字参数: `rate_limit_delay(2.5, jitter=1.0)`
    - 测试断言期望位置参数,与实际调用不匹配

    **验证:**
    - 运行测试: `pytest tests/download/test_ranking_downloader.py::test_download_ranking_rate_limiting -v`
    - 确认测试通过 (应显示 PASSED)
  </action>
  <verify>
    <automated>pytest tests/download/test_ranking_downloader.py::test_download_ranking_rate_limiting -v</automated>
    <manual>测试应显示 PASSED,无断言失败</manual>
  </verify>
  <done>test_download_ranking_rate_limiting 测试断言使用关键字参数 jitter=1.0,测试通过</done>
</task>

</tasks>

<verification>
执行完整的 Phase 6 测试套件,确保修复未引入新问题:

```bash
# 运行所有 Phase 6 相关测试
pytest tests/download/test_ranking_downloader.py -v
pytest tests/download/test_progress_manager.py -v
pytest tests/download/test_retry_handler.py -v
pytest tests/cli/test_validators.py -v
pytest tests/config/test_download_config.py -v

# 确认所有测试通过
# 预期: 所有测试 PASSED
```

检查需求文档一致性:
```bash
# 验证 UX-06 映射已更新
grep "UX-06" .planning/REQUIREMENTS.md
# 应显示: | UX-06 | Phase 6: 多排行榜支持 | Complete |
```
</verification>

<success_criteria>
- [x] REQUIREMENTS.md 中 UX-06 映射更新为 Phase 6 Complete
- [x] test_download_ranking_rate_limiting 测试断言使用 `jitter=1.0` 关键字参数
- [x] 所有 Phase 6 测试通过 (61/61)
- [x] 需求文档 Traceability 准确反映实现状态
</success_criteria>

<output>
创建文件: `.planning/phases/06-multi-ranking-support/06-04-SUMMARY.md`
记录:
- 修复的 2 个 gap
- 测试验证结果
- 需求文档更新内容
</output>
