---
phase: 03-ranking-download
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gallery_dl_auo/download/__init__.py
  - src/gallery_dl_auo/download/file_downloader.py
  - src/gallery_dl_auo/download/rate_limiter.py
  - src/gallery_dl_auo/utils/filename_sanitizer.py
  - tests/download/__init__.py
  - tests/download/test_file_downloader.py
  - tests/download/test_rate_limiter.py
  - tests/utils/test_filename_sanitizer.py
autonomous: true
requirements:
  - CONT-01

must_haves:
  truths:
    - "程序能够流式下载大文件而不导致内存溢出"
    - "下载请求之间有 2-3 秒间隔 + 随机抖动"
    - "文件名中的特殊字符被自动清理 (Windows 兼容)"
    - "单个文件下载失败不会中断整个下载流程"
    - "下载失败时返回清晰的错误信息"
  artifacts:
    - path: "src/gallery_dl_auo/download/file_downloader.py"
      provides: "流式文件下载功能"
      exports: ["download_file"]
      min_lines: 60
    - path: "src/gallery_dl_auo/download/rate_limiter.py"
      provides: "速率控制延迟"
      exports: ["rate_limit_delay"]
      min_lines: 20
    - path: "src/gallery_dl_auo/utils/filename_sanitizer.py"
      provides: "文件名清理工具"
      exports: ["sanitize_filename"]
      min_lines: 30
    - path: "tests/download/test_file_downloader.py"
      provides: "文件下载测试"
      min_lines: 50
    - path: "tests/download/test_rate_limiter.py"
      provides: "速率控制测试"
      min_lines: 30
    - path: "tests/utils/test_filename_sanitizer.py"
      provides: "文件名清理测试"
      min_lines: 40
  key_links:
    - from: "src/gallery_dl_auo/download/file_downloader.py"
      to: "requests"
      via: "stream download"
      pattern: "requests\\.get.*stream=True"
    - from: "src/gallery_dl_auo/download/rate_limiter.py"
      to: "time, random"
      via: "delay calculation"
      pattern: "time\\.sleep.*random"
    - from: "src/gallery_dl_auo/utils/filename_sanitizer.py"
      to: "re"
      via: "character replacement"
      pattern: "re\\.sub"
---

<objective>
创建文件下载、速率控制和文件名清理工具模块,为排行榜下载提供稳健的文件处理能力。

Purpose: 建立可靠的文件下载机制,避免内存溢出、速率限制和跨平台文件名问题。
Output: 可复用的下载工具模块,支持流式下载、速率控制和文件名清理。
</objective>

<execution_context>
@C:/Users/allan716/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/allan716/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ranking-download/03-CONTEXT.md
@.planning/phases/03-ranking-download/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>创建速率控制器</name>
  <files>
    src/gallery_dl_auo/download/__init__.py
    src/gallery_dl_auo/download/rate_limiter.py
    tests/download/__init__.py
    tests/download/test_rate_limiter.py
  </files>
  <action>
实现保守的速率控制策略,避免触发 Pixiv 的 429 错误。

1. **速率控制函数**:
```python
import time
import random

def rate_limit_delay(base_seconds: float = 2.5, jitter: float = 1.0) -> None:
    """
    添加请求间延迟 + 随机抖动,避免速率限制。

    Args:
        base_seconds: 基础延迟秒数 (默认 2.5 秒)
        jitter: 抖动范围 ±秒数 (默认 ±1.0 秒)
    """
    delay = base_seconds + random.uniform(-jitter, jitter)
    time.sleep(max(0, delay))
```

2. **测试**:
   - `test_rate_limit_delay_basic`: 测试基础延迟 (mock time.sleep)
   - `test_rate_limit_delay_with_jitter`: 测试随机抖动 (验证延迟在范围内)
   - `test_rate_limit_delay_negative`: 测试负数延迟保护

基于 CONTEXT.md 决策:
- 固定基础间隔: 2-3 秒 (使用 2.5 作为默认值)
- 随机抖动: ±0.5-1.5 秒 (使用 ±1.0 作为默认值)
  </action>
  <verify>
    <automated>python -m pytest tests/download/test_rate_limiter.py -v</automated>
    <manual>确认速率控制测试通过</manual>
  </verify>
  <done>
- rate_limiter.py 包含 rate_limit_delay 函数
- 默认参数: base_seconds=2.5, jitter=1.0
- 测试覆盖率 100%
  </done>
</task>

<task type="auto">
  <name>创建文件名清理工具</name>
  <files>
    src/gallery_dl_auo/utils/filename_sanitizer.py
    tests/utils/test_filename_sanitizer.py
  </files>
  <action>
实现跨平台兼容的文件名清理工具,移除 Windows 非法字符。

1. **文件名清理函数**:
```python
import re

def sanitize_filename(filename: str, max_length: int = 200) -> str:
    """
    清理文件名,移除 Windows 非法字符。

    Windows 非法字符: < > : " / \\ | ? *
    保留字符: 空格、下划线、连字符、中文

    Args:
        filename: 原始文件名
        max_length: 最大长度限制 (默认 200)

    Returns:
        清理后的安全文件名
    """
    # 移除非法字符
    filename = re.sub(r'[<>:"/\\|?*]', '', filename)
    # 移除首尾空格和点
    filename = filename.strip('. ')
    # 限制长度
    return filename[:max_length]
```

2. **测试场景**:
   - `test_sanitize_basic`: 测试基本文件名 (无变化)
   - `test_sanitize_windows_chars`: 测试移除 Windows 非法字符
   - `test_sanitize_leading_trailing`: 测试移除首尾空格和点
   - `test_sanitize_length_limit`: 测试长度限制
   - `test_sanitize_chinese`: 测试中文字符保留
   - `test_sanitize_path_traversal`: 测试路径遍历攻击防护 (移除 ..)

基于 RESEARCH.md Pattern 4:
- Windows 非法字符: `<>:"/\|?*`
- 保留字符: 空格、下划线、连字符、中文
  </action>
  <verify>
    <automated>python -m pytest tests/utils/test_filename_sanitizer.py -v</automated>
    <manual>确认文件名清理测试通过</manual>
  </verify>
  <done>
- filename_sanitizer.py 包含 sanitize_filename 函数
- 正确处理 Windows 非法字符
- 测试覆盖率 100%,包含中文和路径遍历测试
  </done>
</task>

<task type="auto">
  <name>创建流式文件下载器</name>
  <files>
    src/gallery_dl_auo/download/file_downloader.py
    tests/download/test_file_downloader.py
  </files>
  <action>
实现稳健的文件下载器,使用流式下载避免内存溢出,并处理各种错误情况。

1. **下载函数**:
```python
import requests
from pathlib import Path
from typing import Optional

def download_file(
    url: str,
    filepath: Path,
    timeout: int = 30,
    chunk_size: int = 8192
) -> dict:
    """
    流式下载文件,避免内存溢出。

    Args:
        url: 图片 URL
        filepath: 保存路径 (Path 对象)
        timeout: 超时秒数 (默认 30)
        chunk_size: 分块大小 (默认 8192 字节)

    Returns:
        dict: {
            'success': bool,
            'filepath': str,
            'error': Optional[str]
        }
    """
    result = {
        'success': False,
        'filepath': str(filepath),
        'error': None
    }

    try:
        with requests.get(url, stream=True, timeout=timeout) as response:
            response.raise_for_status()

            # 确保父目录存在
            filepath.parent.mkdir(parents=True, exist_ok=True)

            # 流式写入文件
            with open(filepath, 'wb') as f:
                for chunk in response.iter_content(chunk_size=chunk_size):
                    if chunk:  # 过滤保持连接的新块
                        f.write(chunk)

        result['success'] = True

    except requests.exceptions.Timeout:
        result['error'] = f"下载超时 ({timeout}秒)"
    except requests.exceptions.HTTPError as e:
        result['error'] = f"HTTP 错误: {e.response.status_code}"
    except requests.exceptions.ConnectionError:
        result['error'] = "网络连接失败"
    except OSError as e:
        result['error'] = f"文件写入失败: {e}"

    return result
```

2. **测试场景**:
   - `test_download_success`: 测试成功下载 (mock requests.get)
   - `test_download_timeout`: 测试超时处理
   - `test_download_http_error`: 测试 HTTP 错误处理 (404, 403, 429)
   - `test_download_connection_error`: 测试网络错误处理
   - `test_download_file_write_error`: 测试文件写入错误 (权限、磁盘满)
   - `test_download_creates_parent_dir`: 测试自动创建父目录

实现要点 (基于 RESEARCH.md Pattern 2):
- 使用 `stream=True` 避免内存溢出
- 使用 `iter_content(chunk_size)` 分块写入
- 使用 `response.raise_for_status()` 处理 HTTP 错误
- 返回结果字典而非抛出异常 (优雅降级)
  </action>
  <verify>
    <automated>python -m pytest tests/download/test_file_downloader.py -v</automated>
    <manual>确认文件下载测试通过</manual>
  </verify>
  <done>
- file_downloader.py 包含 download_file 函数
- 支持流式下载 (stream=True)
- 完整的错误处理 (超时、HTTP、连接、文件写入)
- 测试覆盖率 > 90%
  </done>
</task>

</tasks>

<verification>
验证本计划完成的标准:

1. **模块导入**: 所有模块可以成功导入
2. **测试通过**: 所有测试通过 (3 个测试文件)
3. **速率控制**: rate_limit_delay() 执行时确实有延迟
4. **文件名清理**: sanitize_filename() 正确处理 Windows 非法字符
5. **文件下载**: download_file() 返回正确的结果字典
</verification>

<success_criteria>
本计划成功完成的可测量标准:

- [x] rate_limiter.py 实现完成,默认延迟 2.5±1.0 秒
- [x] filename_sanitizer.py 实现完成,清理 Windows 非法字符
- [x] file_downloader.py 实现完成,支持流式下载
- [x] 所有测试通过,总覆盖率 > 90%
- [x] 错误处理完整 (超时、HTTP、连接、文件写入)
- [x] 代码通过 ruff 和 mypy 检查
</success_criteria>

<output>
完成后创建 `.planning/phases/03-ranking-download/03-02-SUMMARY.md` 记录:
- 下载、速率控制和文件名清理的使用方法
- 测试覆盖率和关键测试场景
- 错误处理策略
</output>
