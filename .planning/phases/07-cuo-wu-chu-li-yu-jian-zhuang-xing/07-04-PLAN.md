# Phase 7 Plan 04: 实现断点续传和文件日志系统

**Wave:** 3
**Depends on:** Plan 01, Plan 02, Plan 03
**Files Modified:**
- `src/gallery_dl_auo/download/resume_manager.py` (新增)
- `src/gallery_dl_auo/utils/logging.py` (扩展文件日志)
- `src/gallery_dl_auo/cli/download_cmd.py` (集成断点续传)
- `src/gallery_dl_auo/cli/main.py` (配置文件日志)
- `tests/download/test_resume_manager.py` (新增)

**Autonomous:** true

---

## Objective

实现断点续传机制,程序中断后重新运行能从中断处继续。使用 JSON 状态文件存储当前下载位置,每下载 10 个作品保存一次状态。同时实现结构化文件日志系统,记录所有错误和重试过程到 `~/.gallery-dl-auto/logs/gallery-dl-auto.log`。

---

## Context

**User Decision (LOCKED):**
- 使用 JSON 状态文件存储断点状态
- 状态包含:当前下载位置(作品 ID、索引位置、已下载数量)
- 程序重新运行时自动从断点继续,无需手动指定参数
- 记录错误日志到文件,包含时间戳、错误类型、上下文信息

**Claude's Discretion:**
- 日志文件的位置和命名约定
- Recommendation: `~/.gallery-dl-auto/logs/gallery-dl-auto.log`

**Research Findings:**
- 状态文件应使用临时文件+重命名,避免损坏
- 日志使用 JSON Lines 格式(每行一个 JSON 对象),便于解析
- 断点续传状态:当前索引、总数、已下载/失败数量

**Current Implementation:**
- progress_manager.py 已有基础进度保存,但不支持断点续传
- 日志仅输出到控制台,无文件持久化

---

<tasks>

## Task 1: 创建 ResumeManager 断点续传管理器

<files>
- `src/gallery_dl_auo/download/resume_manager.py`
</files>

<action>
1. 创建 `ResumeManager` 类:
   ```python
   from pydantic import BaseModel
   from pathlib import Path
   import json
   from typing import Optional

   class ResumeState(BaseModel):
       """断点续传状态"""
       mode: str
       date: str
       current_index: int = 0
       total_count: int = 0
       downloaded_count: int = 0
       failed_count: int = 0
       last_illust_id: Optional[int] = None

   class ResumeManager:
       def __init__(self, output_dir: Path, mode: str, date: str):
           self.state_file = output_dir / f"{mode}-{date}" / ".resume.json"
           self.state = self._load_or_create(mode, date)

       def _load_or_create(self, mode: str, date: str) -> ResumeState:
           """加载现有状态或创建新状态"""
           if self.state_file.exists():
               try:
                   with open(self.state_file, 'r', encoding='utf-8') as f:
                       data = json.load(f)
                   return ResumeState(**data)
               except (json.JSONDecodeError, KeyError):
                   # 状态文件损坏,重新开始
                   pass

           return ResumeState(mode=mode, date=date)

       def update(self, index: int, downloaded: int, failed: int, last_illust_id: Optional[int] = None):
           """更新状态"""
           self.state.current_index = index
           self.state.downloaded_count = downloaded
           self.state.failed_count = failed
           self.state.last_illust_id = last_illust_id

       def save(self):
           """保存状态到文件(原子操作)"""
           self.state_file.parent.mkdir(parents=True, exist_ok=True)

           # 临时文件+重命名
           temp_file = self.state_file.with_suffix('.tmp')
           with open(temp_file, 'w', encoding='utf-8') as f:
               json.dump(self.state.model_dump(), f, ensure_ascii=False, indent=2)

           # Windows 兼容:先删除再重命名
           if self.state_file.exists():
               self.state_file.unlink()
           temp_file.rename(self.state_file)

       def should_resume(self) -> bool:
           """判断是否需要恢复"""
           return self.state.current_index > 0

       def get_resume_index(self) -> int:
           """获取恢复起始索引"""
           return self.state.current_index

       def clear(self):
           """清除状态文件(下载完成)"""
           if self.state_file.exists():
               self.state_file.unlink()
   ```

2. 在 `ranking_downloader.py` 中集成:
   - 创建 `ResumeManager` 实例
   - 检查 `should_resume()`,如果 true,从 `get_resume_index()` 开始
   - 每下载 10 个作品调用 `update()` 和 `save()`
   - 下载完成后调用 `clear()`
</action>

<verify>
<automated>pytest tests/download/test_resume_manager.py -x</automated>
<manual>手动中断下载(Ctrl+C),重新运行,验证从断点继续</manual>
<sampling_rate>run after this task commits</sampling_rate>
</verify>

<done>
- ResumeManager 类实现所有方法
- 状态文件使用原子操作保存
- ranking_downloader 集成 ResumeManager
- 中断后重新运行从断点继续
- 下载完成后状态文件被删除
- 单元测试验证断点续传逻辑
</done>

---

## Task 2: 实现结构化文件日志系统

<files>
- `src/gallery_dl_auo/utils/logging.py`
- `src/gallery_dl_auo/cli/main.py`
</files>

<action>
1. 扩展 `logging.py`:
   - 添加 `StructuredFileHandler` 类(继承 `logging.Handler`):
     ```python
     import json
     from datetime import datetime
     from pathlib import Path

     class StructuredFileHandler(logging.Handler):
         """结构化日志处理器:写入 JSON Lines 文件"""

         def __init__(self, log_file: Path):
             super().__init__()
             self.log_file = log_file
             log_file.parent.mkdir(parents=True, exist_ok=True)

         def emit(self, record: logging.LogRecord):
             """写入日志记录"""
             try:
                 log_entry = {
                     "timestamp": datetime.fromtimestamp(record.created).isoformat(),
                     "level": record.levelname,
                     "logger": record.name,
                     "message": record.getMessage(),
                     "module": record.module,
                     "function": record.funcName,
                     "line": record.lineno,
                 }

                 # 添加异常信息
                 if record.exc_info:
                     log_entry["exception"] = self.format(record)

                 # 追加写入(每行一个 JSON)
                 with open(self.log_file, 'a', encoding='utf-8') as f:
                     f.write(json.dumps(log_entry, ensure_ascii=False) + '\n')

             except Exception:
                 self.handleError(record)
     ```

2. 在 `main.py` 中配置文件日志:
   ```python
   from .utils.logging import StructuredFileHandler
   from .config.paths import get_config_dir

   @click.group()
   @click.pass_context
   def main(ctx):
       """配置日志系统"""
       logger = logging.getLogger("gallery_dl_auo")
       logger.setLevel(logging.DEBUG)

       # 控制台输出(用户友好)
       console_handler = RichHandler(show_path=False, show_time=False)
       console_handler.setLevel(logging.INFO)

       # 文件输出(结构化 JSON Lines)
       log_dir = get_config_dir() / "logs"
       file_handler = StructuredFileHandler(log_dir / "gallery-dl-auto.log")
       file_handler.setLevel(logging.DEBUG)

       logger.addHandler(console_handler)
       logger.addHandler(file_handler)

       # ... 现有代码 ...
   ```

3. 日志文件位置: `~/.gallery-dl-auto/logs/gallery-dl-auto.log`
4. 日志轮转:不自动轮转,用户手动清理(或添加配置项限制大小)
</action>

<verify>
<automated>pytest tests/utils/test_logging.py -x</automated>
<manual>运行程序,检查 `~/.gallery-dl-auto/logs/gallery-dl-auto.log` 是否为有效 JSON Lines</manual>
<sampling_rate>run after this task commits</sampling_rate>
</verify>

<done>
- StructuredFileHandler 类实现
- 日志文件创建在 `~/.gallery-dl-auto/logs/gallery-dl-auto.log`
- 日志格式为 JSON Lines(每行一个 JSON 对象)
- 包含 timestamp, level, message, exception 字段
- 单元测试验证日志格式
</done>

---

## Task 3: 集成断点续传和文件日志到 CLI

<files>
- `src/gallery_dl_auo/cli/download_cmd.py`
- `src/gallery_dl_auo/download/ranking_downloader.py`
</files>

<action>
1. 修改 `ranking_downloader.py`:
   ```python
   def download_ranking(...):
       # 初始化断点管理器
       resume_manager = ResumeManager(output_dir, mode, date)

       # 检查是否需要恢复
       start_index = 0
       if resume_manager.should_resume():
           logger.info(
               f"从断点恢复:已下载 {resume_manager.state.downloaded_count}/"
               f"{resume_manager.state.total_count}"
           )
           start_index = resume_manager.get_resume_index()

       # 从断点位置继续
       for idx, illust in enumerate(ranking.illusts[start_index:], start=start_index):
           # ... 下载逻辑 ...

           # 更新状态
           if idx % 10 == 0:
               resume_manager.update(
                   index=idx,
                   downloaded=len(success_list),
                   failed=len(errors),
                   last_illust_id=illust.id
               )
               resume_manager.save()

       # 下载完成,清除状态文件
       resume_manager.clear()
   ```

2. 修改 `download_cmd.py` 捕获中断信号:
   ```python
   import signal

   def handle_interrupt(signum, frame):
       """处理 Ctrl+C 中断"""
       logger.warning("用户中断下载,进度已保存,下次运行将从断点继续")
       print(json.dumps({
           "success": False,
           "error": "USER_INTERRUPT",
           "message": "下载被用户中断,进度已保存"
       }, ensure_ascii=False))
       sys.exit(130)  # 128 + SIGINT(2)

   @click.command()
   def download(...):
       signal.signal(signal.SIGINT, handle_interrupt)

       # ... 现有代码 ...
   ```

3. 更新 help 命令,说明断点续传功能
</action>

<verify>
<automated>pytest tests/cli/test_download_cmd.py::test_resume_after_interrupt -x</automated>
<manual>手动中断下载(Ctrl+C),检查 .resume.json 文件存在,重新运行后从断点继续</manual>
<sampling_rate>run after this task commits</sampling_rate>
</verify>

<done>
- download_cmd.py 捕获 SIGINT 信号
- 中断时进度保存到 .resume.json
- 重新运行时自动从断点继续
- 日志显示 "从断点恢复:已下载 X/Y"
- 下载完成后 .resume.json 被删除
- help 命令更新,说明断点续传
</done>

</tasks>

---

## Success Criteria

- [ ] ResumeManager 类实现
- [ ] 状态文件使用原子操作保存
- [ ] ranking_downloader 集成 ResumeManager
- [ ] 中断后重新运行从断点继续
- [ ] StructuredFileHandler 实现文件日志
- [ ] 日志文件位于 `~/.gallery-dl-auto/logs/gallery-dl-auto.log`
- [ ] 日志格式为 JSON Lines
- [ ] download_cmd.py 捕获 SIGINT 信号
- [ ] help 命令更新,说明断点续传

---

## Must Haves (Goal-Backward Verification)

**Phase Goal:** 程序中断后重新运行能从中断处继续

1. **必须有:** ResumeManager 保存当前下载位置到 .resume.json
   - 验证: 代码审查,确认每 10 个作品调用 save()

2. **必须有:** 中断信号处理保存状态
   - 验证: 单元测试模拟 SIGINT,断言 .resume.json 文件存在

3. **必须有:** 重新运行时检测并恢复断点
   - 验证: 集成测试运行下载,中断,重新运行,断言从断点索引开始

4. **必须有:** 文件日志记录所有错误
   - 验证: 检查日志文件包含 error 级别日志和 exception 字段

---

## Rollback Plan

如果断点续传导致性能问题:
1. 添加配置项 `enable_resume: bool` (默认 true)
2. 如果为 false,禁用断点续传,每次从头开始
3. 保留 ResumeManager 代码,仅跳过调用
