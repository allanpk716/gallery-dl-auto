# Pixiv 排行榜下载器配置文件
# 配置优先级: 命令行参数 > 环境变量 > 配置文件 > 默认值

# 下载配置
save_path: ./downloads
concurrent_downloads: 3
request_interval: 1.0

# 日志配置
log_level: INFO

# 网络配置(未来使用)
api_timeout: 30
max_retries: 3
